# 更新紀錄

格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本號依循 [語意化版本](https://semver.org/lang/zh-TW/)。

## [1.67.0] - 2026-08-23

### 修正

- **裝備的背景敘述整批不生效。** 譯文明明在檔案裡，畫面上還是整段英文。
  不是漏翻，是<b>查不到</b>——而且是兩個各自獨立的破口疊在一起，缺一個修都沒用：

  1. **併起來的行數上限是 8。** 敘述在語料裡是沒有斷行的一整句，遊戲卻依畫面
     寬度切成十來行。語料裡最長的一條 346 個字元、在 tooltip 裡是十一行，
     永遠湊不齊那個鍵。（316 條攤平條目裡有 26 條超過八行，沒有一條超過十二行，
     所以上限改成 14。）
  2. **地名是逐行認的。** `the peak of the Tower of ⏎ Ascension,` 被斷行切開之後，
     兩行各自都不含完整地名，模板裡就留著原樣英文；而語料裡存的是 `{p}`。
     現在整段併回一行之後再認一次，認出來的地名連著樣式一起填回譯文。

- **診斷檔的名額被素材袋吃光。** `majorid-debug.txt` 只記得下二十筆，
  而素材清單的每一行都查不到、每一行又都是一個新起點——同一份清單以
  「整段往後挪一行」的方式佔掉十幾筆，真正要查的那一段從來沒排進去過。

  會被自動斷行的段落一定接在空行後面或從第一行開始，所以起點落在段落中間的
  那些窗格不再記。

## [1.66.0] - 2026-08-23

### 變更

- **Mythic 的名稱改回原文。** 上一版把 82 件 mythic 的名稱翻成了中文，
  這一版全部撤回，遊戲裡看到的還是 `Alkatraz`、`Eschaton`。
  背景敘述<b>不受影響</b>，照樣是中文。
  - 譯名草案沒有丟掉，留在 `docs/mythic-names.md` 等討論定案；
    要套用時再寫進譯文檔。

### 新增

- **fabled 的背景敘述 177 條。** 加上先前的 mythic，兩個最高稀有度的敘述
  都齊了（共 258 條）。
  - 專有名詞（Corkus、Avos、Aelumia、Vulcor Adrenophage…）一律留原文，
    跟 mythic 那批一致。
  - `Astrolabe`、`Dawn Chaser`、`Stalking Mist` 的敘述是自造語言，
    跟 `Restitution` 同一套，社群至今仍在解，<b>刻意留原文</b>。
  - 有八件的原文尾巴黏著一句 `It's pulling towards something.`（中間沒有空格，
    是 Wynncraft 自己資料的樣子），一併照翻。

## [1.65.0] - 2026-08-23

### 新增

- **82 件 mythic 的譯名與背景敘述。** 名稱不走直譯——每個都是從那件裝備自己的
  lore 長出來的。`Alkatraz` 直譯是「惡魔島」，但那個典故在 Wynncraft 裡並不存在；
  它的敘述寫的是一把外表平凡、裡面封著鎖魂咒的巨槌，所以叫「鎖魂囚槌」。
  `Masterwork` 系列統一冠上「匠鑄・」，名字本體沿用原本那一把。
  - 名稱吃 F6 的「翻譯物品名稱」開關，想對得上交易市場與 wiki 的人可以關掉。
  - `Restitution` 的敘述是一整段自造語言，社群至今仍在解，<b>刻意留原文</b>。
  - 譯名整理在 `docs/mythic-names.md`，翻譯團隊要改直接改那一欄。

### 修正

- **五個元素精通的譯名整組錯開了一位。** `Earth Mastery` 顯示成「水元素精通」、
  `Thunder Mastery` 顯示成「地元素精通」，五個全部往後挪了一格。
  同一個檔案裡的 `Earth Damage:` → `地屬性傷害:` 卻是對的，所以錯的只有
  Mastery 這五組名稱與說明——大概是照表格複製時整欄位移。

- **縮排格數不同就查不到譯文。** `Knockback Immunity to Allies` 那一類的細項
  一直維持英文，不是漏翻，是<b>查不到</b>：語料（CDN）裡的縮排是 38、39、40 格，
  遊戲實際送出來的卻是 4 格或 42 格，兩邊字面上永遠不相等。

  縮排幾格取決於畫面寬度與同一組細項裡最長的那一行，是<b>排版</b>不是語意。
  現在剝掉開頭的縮排來查，查到之後把遊戲當下的那一串接回去，對齊不會跑掉。

- **技能樹裡括號的元素分項沒翻。** 畫面上是「總傷害翻了、底下的 `( Air: 60%)`
  沒翻」。原因是 CDN 匯入時把縮排與空格都當成了圖示，語料裡存成
  `{#}({#}{#}Air: +{~})`，跟遊戲送出來的東西對不上。七種元素三種正負號重新產過。

- 補上 `Unlocking will block:`、`Lv. Min:`、`Quest Req:` 等九個缺的標籤，
  以及 shaman 五條只有原文沒有譯文的細項。
- 三張面具的效果行（`Heretic`、`Fanatic`、`Lunatic`）。面具本身的名字先留原文，
  等翻譯團隊的 shaman 檔進來再一起定。

## [1.64.0] - 2026-08-22

### 修正

- **技能敘述裡的數值不會跑到錯的位置了。** 有七條 mage 技能的譯文用的是<b>不帶編號</b>
  的 `{~}`。不帶編號就是照原文順序取，可是中文的語序跟英文不一樣，數值就落到了
  別的位置上——畫面上不會報錯，只會顯示成一個看起來很合理的錯數字：

  ```
  原文  Healing 120% of your max health within 10s
  舊譯  120 秒內治癒量超過你最大生命值的 10 後
  新譯  在 10 秒內治癒量達到你最大生命值的 120% 後
  ```

  Fortitude、Lightweaver、Sunflare、Manastorm、Arcane Restoration 五條是純粹的
  順序對調，改成 `{~1}` `{~2}` 指定位置即可。另外兩條的毛病更深：
  - **Judrajim** 整個條件句不見了。原文的「Mana Bank 達到 140 時」在譯文裡沒有，
    140 被拿去當成「每 140 秒」，而句尾的「提高 20%」其實是<b>下一條技能</b>的內容。
  - **Frozen Tornado** 的原文樣板裡有一半的 `{#}` 其實是空格（CDN 匯入時誤判），
    所以自動校正碰不得，兩邊一起重寫。

  順帶把全部 31 條「有兩個以上數值」的 mage 敘述都用原始數字回代驗過一次。

- **名稱被 tooltip 寬度拆成兩行時仍然翻得出來。** `Dimensional Tear` 斷在行尾，
  第一行只剩 `Dimensional`，於是它成了一個帶樣式的片段。這個片段跟詞典裡的
  `Dimensional Tear` <b>從同一個位置開始</b>，而先前同位置時片段無條件勝——短的贏，
  譯文就卡著半個英文名字，旁邊的 Frozen Tornado 跟 Time Vortex 卻都翻好了。
  現在同位置取比較長的那一個（片段彼此之間本來就是這個規則）。

## [1.63.0] - 2026-08-22

### 修正

- **`ability-terms.json` 裡的詞現在真的會被替換。** 上一版把詞收進去了，
  畫面上卻還是英文——因為<b>扁平檔的載入從來不呼叫 `noteTerm`</b>。
  那些詞「查得到」（所以顏色貼得回去）但沒有登記成<b>可替換的詞</b>，
  兩件事走的是不同的表。
  - 不是每個扁平檔都能當詞典：`gui.json` 收的是整行，全部當成詞會在不相干的
    句子裡亂換（「返回」到處出現）。所以用<b>檔名</b>明說——`*-terms.json` 才收。
- **詞後面黏著圖示也認得出來。** 診斷第 9 筆顯示 Wynncraft 把資源符號跟詞放在
  <b>同一個色段</b>裡：

  ```
  §7adds §f+8§7 Mana to your §bMana Bank ✺
  ```

  藍色那一段是「`Mana Bank ✺`」，連著符號。拿整段去查當然查不到，而使用者看到的
  就是「詞典裡明明有，畫面上還是英文」。現在剝掉尾巴查到之後<b>把尾巴接回去</b>
  ——符號跟詞是同一個色段，一起換掉顏色才會對。

## [1.62.0] - 2026-08-22

### 修正

- **帶顏色的專有名詞翻成中文之後不再掉色**（魔力儲庫、失衡、裂隙之裔、結晶化…）。
  顏色是拿<b>原文的字面</b>去譯文裡找的：`Meteor` 保留英文所以找得到，
  但 `Mana Bank` 在譯文裡是「魔力儲庫」，字面對不上。程式本來就有對策
  （把帶樣式的片段連同<b>它的譯文</b>一起登記），但那要語料裡查得到這個詞才行——
  而 `Mana Bank`、`Unstable`、`Riftspawn`、`Crystallized` 這些
  <b>根本不在語料裡的任何一個檔</b>：它們不是技能名、不是標籤，
  是句子裡的名詞，先前沒有任何一個檔在收。
  - 新增 `ability-terms.json` 專門收這類詞。譯法全部取自翻譯團隊的匯出檔，
    不是自己編的。與 `ability-labels.json` 分開：那個放「標籤:」，這裡放名詞。

### 診斷

- **`line-debug.txt` 現在會印斜體／粗體／底線。** 使用者回報「翻譯出來的標題
  變成斜體」而原文是正的——先前只印顏色，從外面完全看不出斜體是誰加上去的。
  比對原文與譯文那兩段就知道是我們加的還是本來就有。

## [1.61.0] - 2026-08-22

### 語料

- **修好使用者回報「翻了卻沒出現」的那幾條**（引力坍縮、結晶）。譯文一直都在，
  是<b>鍵</b>對不上：`src` 裡多了一個 `{#}`，站在原本該是空格的位置——

  ```
  舊 src  Your Dimensional Tear{#}overloads, growing
  新 src  Your Dimensional Tear overloads, growing
  ```

  上一版的還原規則只認「前面是佔位符右大括號」的情況，於是 `Tear{#}會過載`、
  `Lightweaver{#}命中` 這種（前面是英文字尾）修不到，<b>整條被跳過</b>。
  改成「前後都直接貼著字」就涵蓋了。`({#})` 不會被誤判——左括號不在範圍裡。
- **補上終極計量表的另一個變體。** 遊戲有兩種寫法（`Activate abilities to charge`
  與 `...or heal to charge`），語料兩條都在、但其中一條 `dst` 空著。
  對方的檔案那一行也還是英文，所以照既有的寫法自己補。

### 工具

- **`import-mage-tree.py` 改寫成通用的 `import-ability-tree.py`。**
  翻譯團隊後續還會提供其餘四個職業的匯出檔，工具會自己讀 `properties.classs`
  決定要寫哪個檔，並產出 `docs/<職業>-tree-diff.md`。
- **對照改用技能在樹上的位置，不再需要手工對照表。**
  上一版我說「位置對不起來，89 個只配到 4 個」——<b>那是我解析錯了</b>：
  `tree` 欄位的<b>頁碼</b>被我漏掉，不同頁的技能全擠在一起。補回頁碼、
  再處理「`索引 % 9 == 0` 其實是上一列最後一欄」的換行，mage <b>83 個全部配到</b>。
  - 而且與先前那份<b>獨立</b>用散文語意逐條核對的對照表<b>完全一致（0 分歧）</b>。
    兩種互不相干的方法得到同一個答案，才敢拿它自動處理其餘職業。

## [1.60.0] - 2026-08-22

### 語料

- **對方寫死的數字與圖示都換成佔位符了。** 他們的譯文寫「光球數量增加 2」，
  我們的原文是 `+{~}`；圖示那半他們寫真的字元（⚡ ✺ ✨ 💎），我們的原文是 `{#}`。
  兩種佔位符<b>任一種數量對不上，重建就會整條放棄</b>——畫面上不報錯，
  那條譯文永遠不出現。現在兩種都自動換回去，換完仍對不上的才交給人看。
- **多段敘述改用「這一段提到誰」配對，套用數從 23 條提高到 35 條。**
  先前一律不自動套用，因為兩邊的順序不保證一致。但問題不只是順序，
  <b>斷句位置也不一樣</b>：

  ```
  我們 [0] Heal will now transfer the contents of your Mana Bank...
  我們 [1] Meteor, Ice Snake, and Etheric Slash will add +N Mana...
  他們 (1) 隕石、冰蛇和裂界斬命中敵對目標時，魔力儲庫 ✺ 增加 13 點魔力。
  他們 (2) 冰凍龍捲風和流星雨增加 4 點魔力。
  他們 (3) 治癒不再回復血量，但會提取 魔力儲庫 ✺ 的魔力至玩家身上。
  ```

  他們的 (1)(2) 合起來才是我們的 [1]，而 (3) 是我們的 [0]——順序還是反的。
  每一段都會<b>指名</b>它在講哪個技能，所以改用那個當鑰匙，順序就完全不重要了。
- **補上五個元素精通的翻譯**（使用者回報的 id 11~15）。它們不在 `mage.json`——
  六個職業共用，住在 `shared.json`，而那五條的英文原文<b>早就在語料裡了</b>
  （`Increase your base damage from all Water attacks.`），只是 `dst` 空著。
  所以不必猜英文也不必新增條目，補譯文就好。

## [1.59.0] - 2026-08-22

### 語料

- **併進翻譯團隊的 mage 技能樹匯出檔**（`tools/import-mage-tree.py`）。
  - **83 個技能名稱改成中文**，並拿掉 `keep: "en"`。這是規則變更：先前技能名一律
    保留英文，現在依指示全面中文化。<b>其餘五個職業的名稱還是英文</b>，
    那份匯出檔只涵蓋 mage。
  - 敘述以對方為主：單段且數值對得上的 <b>23 條</b>直接套用，
    其餘 45 條寫進 `docs/mage-tree-diff.md` 交給人挑。
  - <b>只取散文，不取數值標籤。</b>對方的標籤還是英文（`Mana Cost:`、`Range:`），
    我們已經翻好了，整塊照抄等於把英文放回去。
  - <b>數字要換回 `{~}`。</b>對方把數字寫死了（`+{~}` 翻成「增加 2」），
    而佔位符數量對不上時重建會<b>整條放棄</b>——畫面上不報錯，那條譯文永遠不出現。
    換好之後數量仍對不上的，一律不套用。這是測試抓到的。

<h2>對照是怎麼做的</h2>
兩邊沒有機械鍵：流水號是各自的排法（我們照樹上欄位由左到右，他們的編輯器先放
中央主幹），`cellMap` 對我們的 `tree` 89 個裡只有 4 個吻合，而他們的散文
0 條還留著英文。所以 89 → 83 的對照是<b>逐條用散文語意核對</b>出來的，
寫在 `NAMES` 裡。缺的 6 個（五個元素精通、魔力流轉）是我們語料本來就沒有的技能。

### 修正

- **括號註解比正文長時，正文不再被染成註解的顏色。** 技能樹的 `Heal`：

  ```
  Heals you and nearby allies in
  a large area around you.                ← 正文，亮灰，54 字
  (When healing others, you can't heal
  more than 50% of their max health)      ← 括號註解，暗灰，70 字
  ```

  照字數算註解贏，整段譯文就被染成暗灰。但註解依定義是附帶說明，不是這一段的
  主要聲音——現在它<b>不參與底色的統計</b>。不參與之後還得把顏色還回去，
  否則註解變成正文的亮色，那只是把錯誤換一個方向；括號在中英文裡都是註解的記號，
  所以譯文那半也切得出來，連跨行的括號都跟得住。

### 診斷

- `layout-debug.txt` 的詳細判斷額度 12 → 40。坐騎那份先前只有「· 看過」的足跡，
  額度被前面的素材袋與住宅選單用光了。

## [1.58.0] - 2026-08-22

### 診斷

- **`layout-debug.txt` 的詳細判斷從 12 份提高到 40 份。** 1.57 的附加寫入成功了
  （檔案從只有檔頭變成 81KB），但坐騎那份仍然只有「· 看過 Wyvern Flute」的足跡、
  沒有詳細判斷——額度早就被前面的素材袋與住宅選單用光。玩家要滑到想查的東西
  之前，難免先滑過一堆別的。
  - 足跡機制本身證實有效：這一版能明確分辨「沒被呼叫到」與「呼叫了但沒額度」，
    先前那兩件事看起來一模一樣。

## [1.57.0] - 2026-08-22

### 語料修正

- **再修 32 條符號模板——這批是 mage「翻了卻沒出現」的原因。** 有 `dst`、卻永遠
  對不上遊戲算出來的鍵：

  ```
  _raw  You gain +5 Distortion ✹ for every enemy weakened.
  舊 src You gain +{~}{#}Distortion {#} for every enemy weakened.
                       ^^^^ 這個 `{#}` 其實是一個空格
  新 src You gain +{~} Distortion {#} for every enemy weakened.
  ```

  先前的做法是拿壞掉的 `src` 跟 `_raw` 逐段對齊，而多出來的那個 `{#}` 正好把
  段數弄得對不上，於是整條被跳過——<b>要修的東西自己擋住了修法</b>。
  改成直接照 `_raw` 整條重算（它的符號位置與空格本來就是對的），
  再把數字換回 `{~}`。
  - 譯文那邊也跟著抄錯了同一個 `{#}`，一併還原成空格。只改 `src` 不改譯文的話，
    佔位符數量對不上，重建時整條照樣被放棄——那條譯文等於白翻。
  - 只拿掉多餘的，絕不無中生有：重算出來的 `{#}` 比原本多就不動。
- **182 條譯文補上半形間隔。** 原文在括號與符號後面留了一個半形空格，譯文吃掉了：
  `{#}技能範圍: {~} 格(圓形)` → `{#} 技能範圍: {~} 格 (圓形)`。
  只在<b>原文那個位置本來就有空格</b>時補，其餘一律不動（`tools/match-spacing.py`）。

### 診斷

- **`majorid-debug.txt` 的「查不到」不再被同一份 tooltip 灌爆。** 呼叫端會把連續
  幾行併起來、從八行試到兩行，而先前<b>每試一個長度就記一筆</b>——一份八行的
  素材清單灌了十九筆進去，二十個名額用光，真正要查的那一段永遠排不進來。
  改成整輪都失敗才記一次。
- **`layout-debug.txt` 改成附加寫入。** 連續兩版回報「這個檔只有檔頭」。
  先前是把內容累積在緩衝區、每次重寫整份檔案——中途任何一步沒走到，
  整份就停在初始狀態，而且分不出是「沒被呼叫」還是「算到一半掛掉」。
  現在每一份 tooltip 都先落一行「· 看過」的足跡，再視情況寫詳細判斷。
  完全沒有那種行，就確定是判斷根本沒被呼叫到。
  - 跨行查表那一段先前也是重寫整份——兩種寫法混在同一個檔上，後寫的會把
    前面附加的內容整個蓋掉。一併改成附加。

## [1.56.0] - 2026-08-22

### 修正

- **技能敘述整段被畫上底線——真正的原因找到了。** 診斷檔第 18 筆（秘術師流派）
  把數字擺得很清楚：

  ```
  Meteor, Pyrokinesis and Powder Specials      ← 三個底線詞共 32 字
  consume Unstable ⚡ to deal +100% damage.     ← 灰字共 32 字
  ```

  <b>32 比 32，平手</b>。平手時先遇到的勝出，而第一段剛好是 `Meteor` 那個底線詞，
  於是整段譯文全部畫上了底線。1.55 把累計方式從逐行改成逐段，解決了字數懸殊的
  情況（`Halo`），但沒解決<b>平手</b>。
  - 修法不是去調平手規則——那只是把運氣換一邊。改成<b>累計時不看裝飾</b>：
    底線／粗體／斜體是標在特定術語上的例外，依定義就不該當整段的底色。
    「灰＋底線」與「灰」併成同一個，灰以 64 字獨贏，平手根本不會發生。
  - 術語自己的底線仍然回得來——它跟底色不同，會被收成重點段。

### 語料修正

- 再校正 3 條符號模板（`Focus {#}{#}by` → `Focus {#} by` 之類）。
  比對數值時的小數點規則放寬了：`+2.` 先前會把句末的句號一起吃掉，
  整條就被判成「講的不是同一句話」而跳過。
- 符號重算改成<b>一個碼位一個 `{#}`</b>，不再把連續的符號併成一個。技能敘述裡
  連著出現的是元素圖示，每個顏色都不同、遊戲端各給一個佔位符。
  剩下 28 條的 `_raw` 與畫面上的結構對不起來（多半是 CDN 文字沒有前導位移字元），
  自動對不齊，留給人看。

### 診斷

- **`majorid-debug.txt` 先前那一欄是誤導的。** 「挑中的樣式」印的是<b>名稱</b>那一段
  的樣式（`labelStyleOf`），不是整段內文的底色——而整段命中時根本沒用到它。
  看的人會拿它去解釋敘述的顏色，然後查錯方向。現在分成兩欄：
  「譯名的樣式」與「整段內文的底色」，後者才是決定敘述長什麼樣的那一個。
- **`layout-debug.txt` 只剩檔頭的原因找到了。** 整支 `record` 包在<b>一個</b> try 裡，
  `BlockLayout.explain` 一丟例外，連掛號與寫檔都跳過——檔案就永遠停在初始狀態。
  診斷自己失敗卻不留痕跡，比沒有診斷更誤導。現在掛號與寫檔各自獨立，
  中間算不出來就把例外<b>寫進檔案</b>，並在名單標題註明幾份算不出來。

## [1.55.0] - 2026-08-22

### 語料修正

- **`tools/fix-glyph-templates.py`：照 `_raw` 校正被寫壞的 `{#}` 模板，修好 197 條。**
  翻譯團隊回報的「shaman 的符號漏洞」就是這個：

  ```
  _raw : Nearby Bleeding ␣✦␣ enemies will take
  src  : Nearby Bleeding {#}{#}enemies will take
  應為 : Nearby Bleeding {#} enemies will take
  ```

  一個符號變成<b>兩個</b>佔位符，前後的空格還被吃掉一個。兩個後果：查表永遠落空
  （鍵跟遊戲算出來的不一樣），而且譯者看到 `{#}{#}` 會以為真有兩個符號、照著抄進
  譯文。mage 的「部分沒有進行到翻譯」多半也是這個——鍵對不上，翻了也不會出現。
  - 譯文那邊的 `{#}{#}` 一併收斂，否則 src 改了而 dst 沒改，佔位符數量對不上，
    整條會在重建時被放棄，比原本更糟。
  - <b>不碰</b>連續兩個以上符號碼位的條目：`{#}{#}{#}{#}{#}Az Rune` 那種前綴是五個
    不同字型的符號各自成段，從純文字看不出字型，重算會把對的改成錯的。
    剩下 31 條交給人看。
- **`tools/index-numbers.py`：替重複的 `{~}` 補上編號，處理 249 條。**
  `{~}` 是照順序取的，中文語序一調換數值就錯位——畫面上不報錯，只顯示成一個
  看起來很合理的錯數字。編號版 `{~1}`、`{~2}` 直接指名要原文的第幾個。
  今天的行為完全不變（順序一樣），但從此順序是<b>寫明的</b>，譯者調語序時把編號
  跟著搬就好。只處理「原文有兩個以上數值」的條目。

### 修正

- **整段譯文不再被誤畫上底線。** 技能樹的 `Halo`：

  ```
  Increase your Max Orbs        ← 整行灰色，22 字
  from Lightweaver by +2.       ← Lightweaver 帶底線、11 字
  ```

  「哪個樣式代表整段」先前是<b>逐行</b>算的：每一行的主樣式拿走整行的份量。
  第二行裡 Lightweaver 比 from 與 by +2. 都長，於是「底線」成了那一行的主樣式，
  還帶著整行 25 字的份量，壓過第一行的 22 字——整段譯文就全部畫上了底線。
  改成逐<b>段</b>累計：灰 22+5+7=34、底線 11。Major ID 的敘述被染成標題色也是
  同一個算式的另一面。

### 診斷

- **`majorid-debug.txt` 去重。** tooltip 是每一幀重翻一次的，滑鼠停兩秒同一段就被
  記三十次——額度用完，而真正想看的那個 tooltip 一筆都沒進來。回報回來的檔案裡
  30 筆全是同一個 Major ID、20 筆全是同一個綠寶石袋。同樣的內容記一次就夠。
- **`layout-debug.txt` 多了「看過的 tooltip」名單。** 坐騎那份的判斷結果沒出現，
  而額度明明還有——少了名單，「沒被呼叫到」與「呼叫了但被去重擋掉」分不出來。

## [1.54.0] - 2026-08-22

### 修正

- **行內最長的那個彩色詞不再掉色。** 技能樹的 `Larger Mana Bank II` 寫著
  「Increase your maximum / Mana Bank ✺ by +30.」，其中 Mana Bank 是藍的。
  重點段是拿<b>那一行自己的</b>主樣式篩出來的——第二行裡「Mana Bank」比
  「 by +30.」長，於是藍色成了那一行的主樣式，這一段就被當成「不是重點」丟掉，
  譯文重建時沒有東西可以把藍色貼回去。跨行重建現在改用<b>整段</b>的主樣式重挑。
- **Major ID 的敘述不再套上標題的顏色。** 同一個機制的另一面：名稱比同一行露出的
  說明長時，說明那半的灰色被丟掉，整段敘述就染成了標題色，只有數值因為走另一條路
  而保住自己的顏色。跟上一條是同一個修正。
- **別的字型底下的對齊偏移也認得出來了。** 先前的條件是「字型必須是
  `minecraft:space`」，但素材與坐騎的 tooltip 走 `minecraft:language/wynncraft`，
  而那個字型也收了偏移碼位。於是：

  ```
  󏿿󐀁󐀂Defence󏿒󐁤+1 to +2
  [1] 「防禦」 <- 「Defence」  寬 41 -> 18   ← 標籤縮了 23px
  [2] 「󏿒󐁤」                  寬 54 -> 54   ← 間隔沒跟著調整
  ```

  數值整排往左跑 23px。坐騎的「右鍵點擊召喚」原本靠前導偏移置中，也是同一件事。
  放寬之後怎麼確定沒認錯：量出來的寬度<b>剛好等於</b>解碼出來的偏移值，就證明它
  在那個字型底下確實是寬度偏移；材質包若在同一段碼位畫了圖示，寬度對不上，
  不會誤判。

### 變更

- **彩虹字的 Major ID 改回保留原始樣式。** 1.52 加的「把原文那串顏色照比例攤到
  譯文長度上」拿掉了——依使用者指示，不特地去改顏色。譯名維持名稱本來的單一顏色。
  `majorid-debug.txt` 裡「上色方式」那一行也一併移除。

## [1.53.0] - 2026-08-22

### 修正

- **未鑑定裝備的百分比不再被當成 raw。** 鑑定過的寫「Health Regen -20%」，
  未鑑定的寫「Health Regen -39 to -21%」——同一個標籤、同樣是百分比，但百分號掛在
  <b>後面</b>那個數字上。先前的判斷碰到第一個數字（-39）就下結論「這不是百分比」，
  於是整行退回 raw 的譯法：「生命回復百分比」變成「生命回復」，標籤講的跟旁邊的
  數值對不起來。現在會把整行的數值都看過再決定。
- **譯文不再跟數值疊在一起。** Wynncraft 用<b>負的</b>間隔把數值往回貼進標籤尾巴的
  留白，那個留白是照英文寬度算的。中文標籤比英文長時，補償會把間隔算得更負——
  數值被拉進標籤裡，畫面上是兩層字疊著，讀出來是「消耗百分5%」這種東西。
  現在補償只准把間隔放寬，不准收得比原文還緊；譯文變長時數值會往右偏，
  但至少讀得出來。
- **坐騎的屬性列重新對齊了。** 1.51 我替坐騎補的是<b>整行模板</b>，而整行模板的
  優先度高於逐片段——它蓋掉了本來會<b>重算對齊間隔</b>的那條路，把照英文算好的
  位移字元原樣搬到中文上，於是 Acceleration、Toughness 這些長單字的那幾列被推得
  特別右。那九條整行模板拿掉了，八個標籤本來就都在 `ui-labels.json` 裡，
  逐片段走得通，而且它會重算間隔。
  - 這正是上一版寫進 `import-captured.py` 的規則（每個詞都翻得出來的整行不要收）。
    規則寫了，卻沒回頭套用在自己手工加的資料上。
- **彩虹字的判定收緊了。** 門檻從「4 段、每段 2 字」提高到「5 段、每段 1 字」。
  認錯的代價是把一個只是被切碎的名稱染成漸層，那是一眼就看得出不對的錯誤；
  認不出來的代價只是少一個裝飾。兩邊不對等，所以寧可漏認。

### 診斷

- `majorid-debug.txt` 多記一行「上色方式：單色／彩虹」。顏色不對時這一行決定要往
  哪邊查——判成彩虹卻不該是，跟挑錯單色，是兩個完全不同的問題。

## [1.52.0] - 2026-08-22

### 修正

- **對話譯文不再閃爍。** NPC 是一個字一個字打出來的，每一個字都會發一次事件，
  同一句話因此會以幾十種長度進來。先前每一次都重算：半句話查不到就把整框清空，
  下一個字剛好湊夠開頭又冒出來——一句話打完的過程中閃十幾次。
  現在認出來的那條原文會記著，只要新進來的還是它的開頭就直接沿用上一次的結果，
  一整句從頭到尾只算一次。
- **有選項的對話分成兩塊畫。** 選項擺在對話框正上方，跟遊戲原本的上下關係一致。
  先前全部擠在同一框，玩家得自己分辨哪幾行是 NPC 說的、哪幾行是自己要按的號碼。
  認不出選項時<b>不硬分</b>，整段照舊——分錯堆比不分還糟。
- **彩虹字的 Major ID 翻出來也是彩虹字。** 遊戲沒有漸層這種東西，所謂彩虹字是
  一個字切成一段、各給一個顏色硬拼的。先前只取第一段的顏色，整個譯名就變單色。
  現在會把原文那串顏色<b>照比例攤到譯文長度上</b>——原文十五個字、譯名四個字，
  出來仍是同一道彩虹，只是畫得密一點。一整個詞一個顏色的普通排版不會被誤判。

### 整理

- **`quest.json` 拆成三個檔。** 一個檔裡塞著三種東西：任務系統的介面文字、
  任務名稱、沒有歸屬的 NPC 台詞。譯者打開它會看到「Rewards:」跟
  「Let's hope no more grooks waltz in here.」排在一起，語氣與校對標準完全不同。
  - `quest-name.json`（145）任務與地城的名稱
  - `quest-ui.json`（86）開始／完成提示、獎勵欄、目標動詞、進出區域訊息
  - `quest.json`（116）還沒歸到任務的台詞
  - <b>譯文一個字都沒動</b>，只是搬家。
- **玩家帳號名從語料裡清掉了。** 收集器漏進了一個真實帳號名。語料是要推上公開
  倉庫的，那種東西一旦進去就洗不掉。匯入工具也補了一道防線。

### 診斷

- **跨行查表查不到時也會記進 `majorid-debug.txt`。** 先前只記成功的，於是檔案裡
  看不到 Major ID 時分不出是「根本沒被呼叫到」還是「呼叫了但查不到」——這兩件事
  的修法完全不同。現在連攤平後的鍵一起印出來，直接拿去跟譯文檔的 `src` 對。

### 工具

- `tools/import-captured.py` 收緊了三道關卡：裝備名與斷句的半句不收；
  <b>每個詞都已經翻得出來的整行不收</b>（整行模板的優先度高於逐片段，
  補進去最好的情況是沒差別，最壞的情況是蓋掉本來正常的那條路）；
  帶著別人帳號名的不收。對話會照 `ctx` 裡的任務名直接進 `quest/` 對應的檔，
  `speaker` 一併填好。

## [1.51.0] - 2026-08-22

### 新增

- **`tools/import-captured.py`：把玩家收集到的東西併進語料。** 官方 CDN 只給裝備、
  素材與配方；介面文字、坐騎、粉末、Corkian、對話提示<b>都不在裡面</b>。模組其實
  一直在收（`captured.json`），但收完之後沒有路可以進語料，那個檔就躺在那裡。
  - 讀 `captured.json`，把語料還沒有的條目<b>依 domain 自動分類</b>到對應的譯文檔，
    `dst` 留空給人翻。已有的、太短的、沒字母的一律跳過。
  - 用它從一份實際的 `captured.json` 匯入了 <b>263 條</b>。
  - 分不出來的一律進 `misc.json`——寧可集中等人重新歸類，也不要散進錯的檔案，
    那會讓載入順序莫名其妙地打架。
- **坐騎的屬性列翻好了。** `Speed{#}{~}/{~} {#}{#}{#}{#}` 這種整行模板先前不在語料裡，
  只有 `Speed` 這個標籤被逐片段換掉，對齊用的位移字元沒跟著調整，欄位就歪了。
  速度、加速、高度、能量、操控、韌性、增益、訓練九條整行補上。
- **有選項的對話會把原始結構記進 `majorid-debug.txt`。** 遊戲把選項畫在對話框
  <b>上面</b>另一個框裡，但事件只給我們一整串文字，哪幾行是 NPC 說的、哪幾行是
  玩家可以選的，從事件本身看不出來。先把結構記下來，照真實資料實作才不會又猜錯。

## [1.50.0] - 2026-08-22

### 修正

- **Major ID 的顏色查清楚了。** `majorid-debug.txt` 終於送回現場，答案跟我猜的
  都不一樣——遊戲送來的前綴不是 `✦` 而是<b>寬度位移字元</b>，而且名稱前面還掛著
  一個<b>空字串</b>的片段。我先前自己捏的三種測試結構都沒有這兩件事，
  所以三種全過卻修不好。
  - 照真實結構重建測試之後：名稱 `#E0B3E6`、說明 `#AAAAAA`，<b>顏色本來就是對的</b>。
  - 唯一真的不一樣的是<b>冒號</b>：原文的「Transcendence:」連冒號都是名稱的顏色，
    我們的冒號落到說明那半去了，看起來就是「顏色接不起來」。冒號現在併進名稱。

### 新增

- **`misc.json`：遊戲內才有的雜項，68 條。** 官方 CDN 只給裝備、素材與配方，
  粉末、Corkian 增幅器、兌換券、交易市場介面、對話提示<b>都不在裡面</b>，
  只能手工補或靠遊戲內收集。這一批先補上：
  - 粉末的「對武器的效果」「對防具的效果」「鑲嵌 2 個第 4 階以上的粉末…」
  - Corkian 增幅器與模擬器的完整說明
  - 交易市場的設定價格／數量介面
  - 對話提示：「這裡好像沒有人……」、確認、選擇選項、繼續
  - 每日獎勵、技能點數介面、空的插槽

## [1.49.0] - 2026-08-22

### 新增

- **對話不必等講完就會出現譯文。** NPC 的台詞是一個字一個字打出來的，
  打到一半的句子當然不在語料裡——於是譯文要等整句打完才出現，
  而玩家常常已經按 shift 跳過去了。
  - 加了<b>前綴查表</b>：只要開頭夠長（12 個字元）而且<b>只有一條</b>語料
    以它開頭，就可以確定是哪一句，直接把整句譯文先顯示出來。
  - 有兩條以上以它開頭就再等等——寧可晚一點出現，也不要先顯示錯的再跳掉。
  - 說話者也一樣，半句話就認得出是誰在講。
- **翻譯進度改成自動產生。** 進度數字先前寫死在 `README.md` 與 `CONTRIBUTING.md`
  裡，翻了三千條之後那些數字就變成謊話——新來的譯者照著「從哪裡開始翻」的表
  去翻，翻到的卻是早就翻完的檔案。
  - 新工具 `tools/update-docs.py`，內容夾在標記之間，重跑就整段換掉。
  - 掛進 CI：任何譯文變動都會自動更新，不會再過期。

### 修正

- **任務追蹤欄消失時，譯文還留在畫面上。** 追蹤更新事件只在<b>有東西可追蹤</b>
  時才來，玩家切角色或取消追蹤的瞬間追蹤欄整個消失，事件卻不一定會補一則空的。
  現在空內容會直接收掉，另外掛上換角色與換世界的事件一併清乾淨。

### 變更

- **跨行查表的診斷搬到自己的檔案** `config/wynnchayuan/majorid-debug.txt`。
  先前它寄生在 `layout-debug.txt` 裡，共用同一個緩衝、額度與「出事就停」的開關；
  使用者照指示操作三次，回報回來的檔案一次有內容、一次只有檔頭、一次連別的
  內容都沒有——到底是沒被呼叫還是被擦掉，完全分不出來。
  - 現在是<b>附加寫入</b>、自己的額度，而且每一次呼叫都記一個流水號。
    檔案只有檔頭，就代表跨行查表<b>一次都沒被呼叫到</b>——這本身就是答案。

## [1.48.0] - 2026-08-22

### 修正

- **「技能點數:2」數字黏在冒號上——是資料不是程式。** 查表那一層回傳的其實是
  「技能點數: 」（有空格的），但 `gui.json` 裡另外有一條<b>整行模板</b>
  `Ability Points: {~}` → `技能點數:{~}`，冒號後面沒空格，而整行模板優先於
  逐片段，所以它贏了。
  - 用矩陣比對才找出來的：`Ability Points: 2` 沒空格、`Ability Points: +2` 有空格、
    `Cooldown: 3s` 也有空格——同一個標籤兩種結果，那就不是查表的問題。
  - 全語料掃了同一類問題（原文「: 」有空格、譯文沒有），另外修掉 4 條。
- 補上技能樹缺的詞：`(of your max health)`（你的最大生命）、`(of your DPS)`、
  `to Allies`（給友軍）、`to Enemies`（給敵人）、`Click Combo:`（技能組合）。
- `Total Heal:%` 從「總治療量%」改成「總治療量百分比」，跟其他百分比標籤一致。

## [1.47.0] - 2026-08-22

### 修正

- **技能樹有 10 條的數字對調了——這是翻錯，不是程式錯。** 中文語序跟英文不同，
  而佔位符是<b>照原順序</b>填的：`Healing {~} of your max health within {~}s`
  被我譯成「在 {~} 秒內治療了你最大生命的 {~}」，於是 120% 跑到時間欄、
  10 秒跑到治療量欄。Fortitude、Lightweaver、Sunflare、Distortion、Warp Blast、
  Arcane Restoration、Manastorm、Crystallize、Paradox、Tangled Origin 全部中招。
  - 改用<b>指名編號</b>的佔位符（`{~1}`、`{~2}`），順序寫死在譯文裡，
    語序怎麼調都不會再錯。
- **詞彙被替換時失去底線與顏色。** 技能名稱有一套「語料裡只翻一次、
  所有敘述自動跟著換」的機制，但替換出來的字一律用內文樣式畫——
  於是 `Heal` 與 `Arcane Transfer` 的底線在替換的當下就沒了。
  現在會先看那個位置有沒有帶樣式的片段，有就沿用。
- 補上所有流派標籤：光之引導者、秘術師、裂隙行者、墮落者、戰僧、聖騎士、
  神射手、陷阱師、閃電射手、影步者、詭術師、雜技師、召喚師、儀式師、侍祭，
  以及它們的 `Min ... Archetype:` 版本。

### 新增

- **對話會顯示是誰在說話。** 同一個場景裡好幾個 NPC 輪流講的時候，
  光看文字認不出來。語料裡本來就有 `speaker` 欄位，只是先前沒被帶到畫面上。
  現在譯文上方會多一行金色的「Zeph：」。

### 變更

- 技能樹的譯文改用半形括號與分號（跟標籤一致，全形時各吃掉一個中文字寬）。
  中文句子裡的逗號與句號<b>維持全形</b>——半形的 `,` `.` 夾在中文裡很難讀，
  句末的 `.` 更是幾乎看不見。

## [1.46.0] - 2026-08-22

### 修正

- **`layout-debug.txt` 根本沒生成。** 這個檔是查 Major ID 顏色<b>唯一</b>的依據，
  它不寫出來就等於沒有診斷。三個原因疊在一起：
  - 診斷失敗時會把 `file` 設成 `null`——那等於<b>整場遊戲的診斷就此關閉</b>，
    而且第一次失敗發生在什麼時候完全看不出來。改成只記次數，繼續嘗試。
  - `buffer.substring(buffer.length() - 1 - ...)` 會算出負的起點，
    那就是第一次失敗。改成夾在 0 以上。
  - 跨行查表的字串組裝在 `try` <b>外面</b>，一丟例外就會往上傳到繪製層，
    連整個面板都會跟著不見。整段包進 try。
- **要查的那一種偏偏沒被記到。** 先前只記「拆名稱」那條路，但 Major ID
  常常是整段一次命中——改成每一次成功的跨行翻譯都記。
- 診斷檔現在<b>開場就先寫一次</b>。「檔案沒生成」與「生成了但沒內容」是兩件
  不同的事，分不出來的話每次回報都得先猜是哪一種。
- `MajorIdColourTest` 補上三條：診斷檔一開場就存在、跨行翻譯之後有內容、
  而且內容裡確實記下了原文的色段顏色。

## [1.45.0] - 2026-08-22

### 修正

- **譯出來的詞失去原本的樣式（底線、顏色）。** 樣式是拿<b>原文的字面</b>去譯文裡
  找的：`Meteor` 保留英文所以找得到，底線就跟著在；但 `Main Attack` 翻成「普攻」
  之後字面對不上，底線與顏色整個掉了——畫面上原文有底線、譯文沒有。
  - 現在每個帶樣式的片段都會<b>再登記一份譯文版</b>，樣式沿用原本那個。
    兩份都留著：原文版負責沒被翻的情況，譯文版負責翻了的情況。
- **精準命中時，原文首尾的空白被吃掉。** 查表會把鍵 strip 過再比對，命中之後
  直接回傳譯文，於是「Ability Points: 1」變成「技能點數:1」，數字黏在冒號上。
- 補上技能樹缺的標籤：`Required Ability:`（前置技能）、
  `You do not meet the requirements`（你不符合需求條件）、`Total Heal:`（總治療量）、
  `Main Attack`（普攻）、`Archetype:`（流派）等九條。

### 變更

- `The Realm of Light` 從地名清單移除。它是<b>位面</b>不是城鎮，對照表定的是
  「光之領域」，而地名清單會強制保留原文，兩邊互相打架。
  地名清單是自動產生的，重新產生時要記得再拿掉一次——這件事寫進 `GLOSSARY.md` 了。

### 新增

- **翻好 `A New Beginning` 周邊的任務介面**，方便測試：任務名稱本身
  （嶄新的開始），以及所有<b>對話已經翻完</b>的任務名稱——國王的新兵、
  女王的新兵、尋回過往、蛛群登巔、元素演練、尋光、奪塔之役、補給與運送等 18 條。

## [1.44.0] - 2026-08-22

### 修正

- **技能樹在 GitHub 上有譯文，遊戲裡卻一條都沒有。** 上一版把技能樹拆成
  `ability/mage.json` 這種帶資料夾的路徑之後，有兩個地方沒跟上：
  - 下載時 `cacheDir.resolve("ability/mage.json")` 需要 `ability/` 資料夾，
    但沒有人把它建出來——`NoSuchFileException` 被當成「這個來源不通」，
    兩個來源都失敗，那些檔案<b>整個沒下載</b>。
  - 讀取時用的 `Files.list()` <b>不會遞迴</b>，就算下載成功也讀不到。
    改成照 `_index.json` 的清單直接解析路徑。
  - 內建的起始檔複製也一併補上建資料夾。

### 新增

- **技能樹加上代號與技能名稱，並照遊戲版面排序。** 翻譯者先前看到的是一串
  雜湊當鍵，根本不知道「{#}{#}Range: {~} Blocks」是哪個技能的說明。現在每一條都有：
  - `ability`：屬於哪個技能，跨技能共用的會全部列出來
  - `code`：遊戲版面上的位置，例如 `MAGE-P1-R00-C04`（法師，第 1 頁，第 0 列，第 4 欄）
  - 排序照職業 → 頁 → 列 → 欄，同一個技能的名稱、說明、數值排在一起
  - 工具：`tools/tag-abilities.py`
- **跨行查表的專屬診斷。** Major ID 的名稱顏色我已經修錯兩次，兩次都是照畫面
  猜原文的色段長怎樣然後猜錯。現在 `layout-debug.txt` 會直接寫下現場：
  原文第一行<b>每一段的文字與顏色</b>、查到的譯名、程式最後挑中的樣式。
  三者擺在一起，錯在哪一步一眼就看得到。
  - 挑樣式的判斷抽成同一支給診斷與正式路徑共用——兩邊各寫一次的話，
    診斷會說「挑到粉紅色」而畫面上是白的。

## [1.43.0] - 2026-08-22

### 修正

- **Major ID 的名稱顏色跟說明對調了。** 上一版挑樣式是從「跟整行主要樣式不同」
  的那些片段裡挑；但名稱如果比同一行露出的說明還長，主要樣式就變成名稱本身，
  被判為「不同」的反而是說明——於是<b>兩邊顏色互換</b>。
  - 改成直接從原文第一行取<b>第一段有字母的</b>樣式，那一段就是名稱本身，
    不再依賴任何「誰是主要樣式」的推算。
  - 測試補上第三種情境（名稱比說明長），三種色段結構現在都釘住了。

### 變更

- **技能樹依職業拆開了。** `translations/ability/{warrior,mage,archer,assassin,shaman}.json`，
  外加 `shared.json`（不只一個職業共用的條目）。原本的 `ability.json` 移除。
- **`Range` 與 `Area of Effect` 分清楚了**：`Range` 是「施放範圍」（能打多遠），
  `Area of Effect` 是「技能範圍」（涵蓋多大一塊）。這兩個先前都翻成「範圍」。
- **法術名稱改回英文**：費用欄從「隕石消耗」改成 `Meteor 消耗`，二十個法術
  全部比照辦理。既有的 65 條譯文用新工具一起改掉了。
- **技能名稱刻意保留英文**，並在資料裡標記 `keep: en`，不再算進翻譯進度——
  否則那幾個檔案永遠停在八成，看不出真正還剩多少。

### 新增

- **法師技能樹的說明全部翻完，111 條。** 從 Meteor、Ophanim 到最終的
  Dawn、Gravitational Collapse。資源與狀態名稱（Distortion、Crystallized、
  Unstable、Shining、Mana Bank ✺）保留英文，因為它們在遊戲裡跟圖示綁在一起。
- **`tools/apply-glossary.py`**：對照表改了之後，把既有譯文一起改掉。
  `GLOSSARY.md` 是準則，但改了對照表，已經翻好的幾千條不會自己跟著變，
  而這種不一致在遊戲裡看不出來，只有玩家會覺得「怎麼一下叫這個一下叫那個」。
- 進度表改成掃子資料夾——技能樹拆開之後，只掃頂層會整片漏掉一千多條。

## [1.42.0] - 2026-08-22

### 修正

- **Major ID 的名稱顏色不見了。** 遊戲把 `✦` 跟名稱分成<b>兩個色段</b>送過來，
  而「純符號的段也算重點段」是 v1.33.1 為了保住 `✖` 的紅色才加的——兩件事
  湊在一起，程式挑樣式時挑到的就變成那個 `✦`，名稱於是套上了 `✦` 的顏色。
  改成挑<b>有字母的</b>那一段。
  - 加了 `MajorIdColourTest`，用真正的語料跑：`✦` 併在名稱裡、以及 `✦`
    自成一段，兩種都要保住名稱自己的顏色。寫測試的過程本身就重現了這個 bug。

### 新增

- **開始翻裝備 lore，91 條。** 先挑<b>帶劇情線索</b>的那一批——提到 Fruma、
  Dern、衰朽、腐化、下界、村民、矮人、Bovemist、Theorick、Bob、Remikas 的都在裡面。
  - Bob 的胸甲、Theorick 的冰甲、Remikas 的護腕（裡面封著他的靈魂）、
    傳說神殿守衛的盾、Tisaun 獨自擋下一場圍城的那件裝備。
  - `It's pulling towards something.`（它正被什麼東西牽引著。）
    這句接在句尾，有它的裝備都還有未揭露的內容。
  - **`Palimpsest` 刻意留空不翻**——那段敘述是 Fruman 文字的<b>密文</b>，
    社群還在解，翻了就毀掉整個謎題。這件事寫進 `GLOSSARY.md` 了。
- 對照表新增「裝備 lore」一節：下界、守衛魔像、傳說神殿、村民、獸人／綠皮、
  Bovemist、Dernel 部族、Avos、WynnExcavation。

## [1.41.0] - 2026-08-22

### 修正

- **翻譯面板不出現的真兇找到了。** `error-debug.txt` 直接指出來：
  折行時如果換行點<b>剛好落在一個空白上</b>，切點 `cut` 就等於當前位置 `i`，
  而 `lineStart` 被設成 `cut + 1`——比 `i` 還大。接下來的
  `substring(lineStart, i)` 變成 `substring(24, 23)`，當場丟出
  `StringIndexOutOfBoundsException`。
  - 那個例外被繪製層的 `catch (Throwable)` 吞掉，結果是整個面板都不畫。
    畫面上看起來像「這件物品沒翻到」，實際上語料好好地躺在那裡。
  - 加了 `WrapTest`：那個確切的形狀、極端寬度、以及四千輪亂數，
    都不能丟例外，而且折完的內容去掉空白之後必須跟原文一模一樣。
- **名牌徽章的 NullPointerException。** 名牌的附著點可能是 null（玩家還在載入、
  或實體剛好在這一幀被移除）。每幀跑好幾次，一場遊戲累積了一萬五千次。
- **武器 tooltip 的欄位對齊被上一版弄壞了。** 上一版用「整份 tooltip 裡有沒有
  別的行的數值落在同一個 x」來判斷欄位，但 Wynncraft 的留白是<b>寬度位移
  字元</b>不是空白，用純文字量根本量不準。
  - 改用兩個介面實際的差別：**標籤帶不帶冒號**。
    技能樹是 `Fire Damage: +15%`（數值緊跟標籤，各行不對齊）；
    物品欄是 `Main Attack Damage -17%`（數值排在同一欄）。
  - 這樣 `Main Attack Damage` 這種「最長的標籤剛好把數值頂到定位、
    中間只剩一個空格」的行會照常補償，不會脫離其他行的欄位。

### 新增

- **翻完 `Arachnids' Ascent`，111 句。** Wynn 同伴線的收尾：三人一起攻打
  Nivla 的蜘蛛巢，玩家要選 Aledar 或 Tasim 一起上火山。最後三個人在兵營
  舉杯——「敬一段長久而充實的友誼」。讀過 `Queen's Recruit` 就知道那句有多重。
  `Mushroom Man` 裡被抓走的 Yahya 也在這裡獲救。

## [1.40.0] - 2026-08-22

### 新增

- **翻完 Wynn 的整條同伴線，557 句。** 從 `King's Recruit` 之後一路到記憶恢復，
  正好接上已經翻好的 `Queen's Recruit`。校稿時是一段連續的故事：
  - `Elemental Exercise`（65）、`Infested Plants`（33）、`Mushroom Man`（75）、
    `Supply and Delivery`（76）、`Taking the Tower`（44）、`Recover the Past`（264）
- **`Recover the Past` 是整條線的核心。** 它揭露 Fruma 送去 Wynn 的「新兵」，
  其實是被抹除記憶、流放出境的罪犯與受詛者。Tasim 是因為救了 Aledar 才被抓——
  Aledar 醒來的第一句話是「哦。你——你是受詛的」。
  - 新定的譯名：`the Adjustor` 調律器、`Inquisitor` 審訊官、
    `memory restorification orb` 記憶復原化球（`restorification` 是 Picard
    自己造的字，中文也留了造字感）。
  - 這一段全部寫進 `docs/world-and-story.md` 的第一幕，附原句。
- 對照表補上八個角色的語氣：Caid、Yahya、Ope、Alvin 鎮長、Ragon 隊長、
  Aegis 上將等。Yahya 的口吃與 Zeph 用同一套處理。

### 修正

- **上一版補 `{u}` 的規則太寬，補錯了一部分。** 「逗號前面有空白」會誤判——
  `my weapon only uses , right?` 少的是<b>元素名</b>不是玩家名字，補上 `{u}`
  等於把譯文寫錯。規則收緊成「逗號前面必須是句末標點」，從上一版重新推導，
  222 條改成 182 條。少補幾條沒關係，補錯不行。
- **清掉 10 條殘留的維基標記**（`{{RenderLocation`、`[[連結|顯示文字]]`、
  多出來的 `}}`）。那些標記在遊戲畫面上都不存在，所以那些條目一樣永遠對不上。
  新工具 `tools/strip-wiki-markup.py`。
- `Taking the Tower` 與 `Supply and Delivery` 各有一條把<b>兩句台詞黏成一句</b>，
  已拆回兩條。

## [1.39.0] - 2026-08-22

### 新增

- **翻完光之領域那條線，191 句。** `Finding the Light`（99 句）與
  `The Realm of Light`（92 句）——Gavel 的主線，也是整個世界觀的核心：
  Orphion、光之領域、森林守護者，以及守了一千年的 Lari。
  - Lari 的弧線是「我投入一千年，你幾週就做完了」的不甘心，最後她為此
    對玩家動了手。她從端莊講到失控嘶吼，語氣是一路變的，譯文跟著變。
  - Orphion 稱呼玩家用的是 `my pulse`，譯成「我的脈動」。那是刻意的疏離感，
    不要改成「我的孩子」之類順口的說法。
  - `achara` 是 Lari 對玩家的稱呼，Gavel 語，保留原文——原文本來就是外語。
  - 新定的譯名：光之領域、光之樞紐、巨石碑、衰朽、高階 Gavel 語。

### 修正

- **222 條任務台詞的玩家名字被抓取階段吃掉了。** 維基的對話表格裡玩家名字
  那一格是空的，抓下來就變成「...ah. . So you are needed here too」。
  - 這不只是難看：遊戲裡那個位置會被抽成 `{u}` 佔位符，所以少了 `{u}` 的
    條目<b>永遠對不上</b>，翻得再好也不會顯示。
  - 新工具 `tools/fix-player-name.py` 用「英文本來不會這樣寫」的標點組合
    判斷，補回 222 條，散在 41 個任務檔裡。
  - 刻意不動兩種：被吃掉的其實是元素名而非玩家名的（`Strength, , Intelligence`
    這種一行要補兩個的一律跳過），以及本來就寫壞的台詞（`.;I. ,told;.`
    標點比字母還多，那種殘缺是氣氛的一部分，修好等於把演出抹掉）。

## [1.38.0] - 2026-08-22

### 修正

- **翻譯面板有時整個不出現。** 使用者的 `captured.json` 裡躺著
  `render.tooltipError` 1,398 次、`render.badgeError` 15,225 次——繪製的例外
  被 `catch (Throwable)` 吞掉，只記了一個次數。例外一丟，面板就整個不畫，
  畫面上看起來像「這件物品沒翻到」，但語料明明有。
  - 包住例外是對的（一個算繪例外不該拖垮畫面），但只記次數等於沒記。
  - 現在會把堆疊寫進 `config/wynnchayuan/error-debug.txt`，同一種只記一次。
    **請把這個檔一起回報**，才知道那幾千次到底是什麼。
- **就地取代模式翻不到跨行的東西。** Major ID 與技能敘述的原文<b>本來就是
  一整段</b>，逐行查表永遠查不到。面板模式有做跨行查表，就地取代卻自己寫了
  一個逐行迴圈——同一份語料、同一件物品，換個模式就變回英文。
  兩條路徑現在共用同一套 `translateLines`。
- **每一條詞條都被補上一大塊空白。** 判斷「這是不是對齊欄」原本只看單行：
  數值以 `+`／`-` 開頭就算欄位。於是中文標籤比英文短的時候，補償就是一段
  空白，畫面上變成「火屬性傷害:␣␣␣␣␣+3-5」，間隔跟沒翻譯時一樣寬。
  - 真正的欄位是伺服器<b>刻意排出來的</b>：短標籤後面會塞一段明顯的留白，
    把數值頂到定位。所以改成整塊判斷——先找出這份 tooltip 裡「留白兩格以上」
    的行，它們的數值 x 才是欄位位置；其他行只有在數值剛好落在同一個 x 上
    才算同一欄。
  - `Elemental Spell Damage +372` 是後者（標籤長到剛好頂上欄位，中間只剩一個
    空格，但確實對齊），照樣補償。`Main Attack Damage: +5%` 不是，不再補。

## [1.37.0] - 2026-08-22

### 新增

- **翻完 `A New Beginning`，289 句。** 直接接在 `Queen's Recruit` 後面——
  被趕出 Fruma 之後，玩家自己成了全境通緝的「受詛者」，在 Espren 被 Zeph 撿到，
  加入 Syndra 的反抗組織「風中之葉」。最後 Kaio 在墳前那一段講的是 Aledar，
  那是整個任務的重心。
- **Zeph 的口吃翻出來了。** 原文用連字號重複字首（`th-there`、`w-wondering`），
  中文用重複首字加頓號：「你、你在這裡啊」。每一句都有，但控制在讀得下去的密度。
- Zhiraok 的數學梗延續：`crunch the numbers`（算那些數字）、
  `factored out`（因式分解）、`one positive in a sea of negatives`（一堆負數裡
  總算有一個正的）、道別語 `Keep it constant`（保持常數）。全部進了對照表。
- 角色語氣表補上 Syndra、Zeph、Rex、Mora、Sui、Kaio。

## [1.36.0] - 2026-08-22

### 修正

- **滿滾的物品翻譯整個消失。** 詞條全部滾到最高時，遊戲會在名稱前面加一個
  `Perfect`（最低是 `Defective`）。語料裡有 `Ephemeral Tome of Mysticism II`，
  但畫面上是 `Perfect Ephemeral Tome of Mysticism II`——多出來的那個字讓整個
  名稱查不到。平常翻得好好的，一拿到滿滾的東西就沒了。
  - 剝掉前綴查名稱，再把前綴的譯文接回去。兩邊都查得到才算數，只中一半就
    寧可整行不翻，免得中英混在一起。
  - `Perfect`／`Defective` 的譯文放在 `ui-labels.json`，翻譯團隊可以改。
- **技能樹的百分比標籤從來沒生效過。** 百分比的鍵是 `Fire Damage:%`，但程式是
  拿整個模板去接 `%`。模板尾端有一個空格，接出來變成 `Fire Damage: %`，
  對不到，於是<b>靜默</b>退回非百分比的譯法——`+15%` 那一行被標成
  「火屬性傷害」而不是「火屬性傷害百分比」。看起來只像翻得不夠好。
- **`gui.json` 蓋掉了 `ability-labels.json` 的 9 個鍵。** 兩份的值一直是一樣的，
  所以以前看不出來；但技能樹改用自己的寫法之後，改 `ability-labels.json`
  就會沒反應。重複的那 9 個從 `gui.json` 移掉了。

### 變更

- **技能樹改用半形標點與「百分比」。** 依專案維護者的排版決定：
  `火屬性傷害%：` 改成 `火屬性傷害百分比: `，全形的 `：（）` 一律改半形。
  `ability.json` 196 條、`ability-labels.json` 全數、`gui.json` 22 處、
  `wynntils.json` 5 處。
  - 量過了：160 條標籤有 23 條譯文比原文寬，最多的是「連根拔起消耗百分比:」
    比 `Uproot Cost:` 寬 21px。這些行的數值會往右挪，但不會疊在一起。
  - 半形冒號只有 2px 寬、右邊也沒有全形字自帶的留白，所以間隔下限從 4px
    提到 6px，免得數值看起來黏在冒號上。

## [1.35.0] - 2026-08-22

### 新增

- **翻完 `Queen's Recruit`，673 句。** 這是 `King's Recruit` 的正統續篇，也是
  Fruma 篇主線的開場——被流放的新兵被女王請了回去，名義上是慶典，實際上是
  一場篩選。整個任務從頭到尾一句不漏。
- **Fruma 篇的專有名詞定了下來**，寫進 `GLOSSARY.md`：`Sovereign` 是「統領」、
  `Gendarme` 是「憲兵」、`the Cursed` 是「受詛者」（關鍵劇情詞）、
  `blight` 是「災厄」。人名與家族名照慣例保留原文。
- **Zhiraok Nasin 滿嘴數學這件事翻出來了。** 這個角色的每一句台詞都塞著一個
  數學詞——罵人用「無知的被積函數」、稱讚時機用「黃金比例」、把人趕走說
  「減掉」、算錯了說「誤算」。最後那句 `how much more irrational can we get`
  同時是「不理性」與「無理數」，中文的「無理」剛好兩層都接得住。
  這些全部列在 GLOSSARY 的雙關表裡，之後的主線要照著接。
- 角色語氣表補上 Fruma 篇的九個人（Mel 的鄉下口音、Picard 的亢奮、
  Reynauld 的疲憊、女王的從容）。

## [1.34.0] - 2026-08-22

### 新增

- **技能樹的數值敘述補完。** 198 條 `stat` 模板全部有譯文了，先前只完成 96 條。
  「隕石傷害：+30%」「圖騰消耗：-1」「Tempest 額外彈跳：2」這類敘述，
  現在點開任何一個技能都看得到中文。
  - 招式名沿用先前費用欄已經定下來的譯法（隕石、連擊、圖騰、牽引、光環），
    技能本身的名字維持英文，方便跟人溝通。
  - 距離用「格」、時間用「秒」，符號 `(⚔) (❈) (☗) (➲) (❁)` 原樣保留。

### 修正

- **技能樹裡兩種冒號並存。** 「持續時間：」是全形、「傷害: 」是半形，
  間距也差一截。原因是查表剝尾巴時<b>把冒號跟空白一起剝掉</b>——
  `ability-labels.json` 的鍵本來就帶冒號（`"Damage:"`，技能樹要比物品欄更短的
  譯法），冒號被剝掉之後那個鍵永遠對不到，退回物品欄的無冒號版，再把原文的
  半形「`: `」接回去。
  - 改成<b>先留著冒號查一次</b>，對不到才剝。
  - 真的沒有專用鍵的標籤（`Mana Regen:`）也不再接回半形冒號，一律換成全形、
    後面不留空白。
  - 補上 `Effects:`、`Mana Regen:` 兩個技能樹標籤。
  - 加了一條測試把這件事釘住，用真正的語料跑。
- **對照表檢查誤判。** GLOSSARY 裡除了術語表，還有角色語氣、雙關說明那幾張表；
  檢查工具把它們也當成術語讀進去，於是 npc.json 的譯名被指控「沒照對照表翻」。
  改成只讀表頭是「原文 | 譯文」的那張。
- 全形標點後面刻意不接空格，這是模組本來的行為，檢查工具不該再為此警告。

## [1.33.1] - 2026-08-21

### 修正

- **上一版的全形冒號修正修錯了地方。** 我改的是整行查表那條路，但技能樹走的是
  <b>逐片段</b>——那條路自己把原文的前後空白接回去，完全沒經過上一版的修正。
  所以「總傷害：␣」照樣多一塊留白。這次改在對的地方。
- **符號的顏色被吃掉。** `✖ Ability Points: 1` 在原文是<b>三段</b>：
  紅色的 `✖`、灰色的標籤、白色的數值；重建之後變成兩段，`✖` 跟標籤併在一起，
  紅色就沒了。
  - 樣式沿用本來就有處理這種事，但它要求「至少三個字元且含字母」——
    那是為了避免短詞在譯文裡到處誤中。`✖` 只有一個字元，被擋掉了。
  - 符號不適用那條限制：它原樣留在譯文裡，比對不會出錯。改成<b>整段都是符號</b>
    就收，不管多短。

## [1.33.0] - 2026-08-21

### 修正

- **技能樹到處都是莫名的空格——找到了，是全形冒號。** 用診斷的數字算得出來：
  「總傷害：␣」＝ 27 px（三個中文）＋ 9 px（全形冒號）＋ 4 px（空格）。
  <b>全形冒號本身右半邊就是留白</b>，我們又把原文 `"Total Damage: "` 尾端的
  半形空格照樣接了回去——留白疊留白，間隔變成兩倍。
  - 技能樹整片都是「標籤: 數值」，每一行都多這麼一塊，看起來就是到處有空格。
  - 改成<b>全形標點後面不接空白</b>，中文排版本來也就不這樣寫。
    整行模板的 101 條譯文一併修掉。

### 診斷

- **逐片段的輸出加上顏色。** 顏色掉了在畫面上很明顯，但診斷完全看不到，
  只能靠截圖猜——猜了好幾輪都沒猜中。現在每一段都會印出
  `色 #55FF55 -> #AAAAAA` 這樣的對照。

## [1.32.0] - 2026-08-21

### 新增

- **技能樹的詞條改用<b>整行</b>譯文。** `ability.json` 裡有 198 條 
  `role: stat` 的整行模板，先前一條都沒翻，所以每一行都得走「逐片段重組」——
  那正是間隔跑掉的來源。整行有譯文就整行替換，重組根本不會發生。
  照 `ui-labels` 與 `ability-labels` 自動產生了 96 條，其餘 102 條尾巴帶特殊
  說明的留給人工（自動猜只會猜錯）。

### 修正

- **攤平查表的譯文整段掉色。** 重建時整段共用一個樣式，先前取<b>第一行</b>的——
  但第一行常常是「名稱：」那半，顏色跟後面的說明不一樣（Major ID 的名稱是
  粉紅的、技能說明是綠的），於是整段都被染成名稱的顏色。改成看整段：
  哪個樣式涵蓋的字最多就用哪個。

## [1.31.0] - 2026-08-21

### 新增

- **模組有 icon 了。** 原圖是 1023×1537 的直式，模組 icon 必須是正方形——
  去掉四周透明區域之後<b>置中補成正方</b>再縮，直接拉伸會讓圖變形。
  256×256 進 jar，512×512 給 README 與 Modrinth。
- **技能樹的法術專屬標籤 54 條**（`Tornado Duration:`、`Slow Duration:`、
  `(Circle-Shaped)` 這類）。只翻通用的 `Duration:` 會讓它們夾在已翻的行中間
  特別突兀。

## [1.30.0] - 2026-08-21

### 修正

- **看著戰鬥假人時譯文框一直閃。** 判斷是<b>每幀</b>做的，來源的字變一下、
  或準心晃開一格，框就收掉再出現；假人的字每被打一次就換一個數字，
  於是以每秒好幾次的頻率閃。加上<b>最短顯示時間</b>（0.6 秒）——
  框出現得比人看得完還快就消失，比不顯示更糟。換內容才重新計時。
- **技能樹的括號內元素標籤太寬。** `( Air: 10%)` 原文只有 20 px，
  「風屬性：」是 40 px，整行反而變寬 20 px——那正是回報的「格式錯亂」。
  前面就有元素圖示了，改用一個字：`風：`、`水：`、`火：`。
- **`Blocks`、`3s` 這些單位沒翻。** 它們是<b>數值那一段的一部分</b>
  （`18 Blocks` 是一整段），要用 `{~} Blocks` 這種帶佔位符的鍵才查得到。
- `Effect:` 先前用了半形冒號，跟其餘不一致。

### 說明

- 技能樹的「莫名空格」查清楚了：法術的 tooltip <b>沒有任何排版偏移字元</b>
  （`Mana Cost: 50` 整行只有圖示、空格、標籤、數值四段），所以我們一個像素
  都沒有加。有間隔的地方是 Wynncraft 自己的欄位對齊，中文標籤比英文短了
  四十幾像素，那個間隔就顯得大。

## [1.29.1] - 2026-08-21

### 修正

- **每個翻譯 PR 都被 CI 擋掉。** `quest-dialogue.json` 是從 `quest/` 底下<b>產生</b>的，
  內容本來就是副本；譯者改了任務檔而合併檔還是舊的，跨檔重複檢查就報
  「兩份譯法不同」。產生物參與這種檢查必然誤報——改用 `_meta.generated` 標記並跳過。
  - 而且譯者多半在 GitHub 網頁上改，手邊沒有 Python，本來就不可能自己重新產生。
    合併改由 CI 做：合進 `main` 之後自動重新產生合併檔與認領清單。
- **兩個錯譯。** `Aldorei's Secret Part II` 被翻成「第一部」；
  `Athletic Tome of Weapon Mastery III` 套到了 `Nimble Combat` 的譯名。
- **技能樹裡的元素標籤沒翻**（`Fire:`、`Water:` 那些），夾在已翻的行中間特別突兀。

### 診斷

- **優先記「有問題」的行。** 已經對齊好的行只留 10 筆當對照——一份裝備 tooltip
  就有三十幾行是「守住」的，額度被它們佔光，真正歪掉的技能樹一行都排不進來，
  連續五次回報都是這樣。

## [1.29.0] - 2026-08-21

### 新增

- **`ability-labels.json`：技能樹專用的詞條標籤。** 技能樹的標籤<b>帶冒號</b>
  （`Fire Damage:`），跟物品 tooltip 的鍵不一樣，所以可以各自用各自的寫法。
  這裡刻意用短的「%」而不是「百分比」——技能樹的欄位窄，長標籤會把數值擠出框外。
- **翻好兩個任務當範例**：Cook Assistant（17 句）、A Grave Mistake（16 句）。
  廚子是碎念的抱怨角色，語氣就照那樣寫；墓園那個是恐怖向的，
  原文<b>刻意</b>殘缺的台詞譯文也保留殘缺，補通順會把氣氛抹掉。
- **`GLOSSARY.md` 補了「有雙關或典故的」一節。** `A Grave Mistake` 的 `grave` 
  同時是「嚴重的」與「墳墓」，玩家最後真的掉進一座墳墓——譯成「致命的失足」
  兩層都接得住。做不出來的就註明原文在玩什麼，讓譯者的語氣接得上。

### 修正

- **面板折行把 Nori、Wynnpool 的進度條拆散了。** 那些條是一整串排版符號，
  中間斷開就散了。折行先退掉——譯文過寬要處理，但不能用「照寬度硬折」
  這種對內容一無所知的做法。
- **診斷額度從 40 加到 120。** 一份裝備 tooltip 就吃掉三十幾筆，玩家滑過兩件
  裝備額度就滿了，真正要看的技能樹一行都排不進來——連續四次回報都是這樣。

## [1.28.1] - 2026-08-21

### 新增

- **任務對話改成一個任務一個檔案**（`translations/quest/`）。兩萬多句擠在一個
  JSON 裡，打開就卡、搜尋很慢，兩個人同時改還會在同一個檔案裡打架。
  - 模組讀的仍然是合併後的 `quest-dialogue.json`——譯文是啟動時一個一個抓的，
    157 個檔就是 157 次連線。那個檔是<b>產生物</b>，改完跑 `tools/quest-bundle.py`，
    CI 會擋住忘記重新產生的情況。
- **技能樹的介面字串。** `Blocked by:`、`Ability Points:`、`Currently Selected`、
  各職業的技能頁標題等 21 條。被阻擋的技能先前整塊都是英文。

### 修正

- **技能樹的譯文黏在一起。** `風屬性傷害百分比+15%` 中間沒有空隙。間隔的下限
  在 `alignColumns` 夾過了，但後面的 `settle` 會再動一次同一個空白，
  而那裡沒夾——譯文比原文寬時就被收成 0。
- **看著戰鬥假人時譯文框還是會閃。** 先前只比「是不是同一個實體」，
  但假人的名牌實體會整個換掉。改成也比<b>模板</b>：只有數字不同就算同一句。

## [1.28.0] - 2026-08-21

### 新增

- **任務對話補到 22,088 句。** wiki 上有<b>兩種</b>對話寫法，而且一頁只用一種。
  先前只認 `{{Dialogue}}` 模板，用 `*'''說話的人:'''` 那種寫法的頁面
  一句台詞都沒抓到——而那是一半以上的頁面。157 個任務、862 個角色。
- **`docs/quest-dialogue.md`：任務對話認領清單。** 每個任務幾句、翻了多少，
  外加「適合第一次接的」小任務清單。三萬句放在同一個檔案裡，打開只會看到
  一片 JSON，不知道從哪裡下手。由 `tools/quest-index.py` 產生。

### 修正

- **譯文太長時面板會超出畫面。** 先前不設上限，一路長到畫面外之後被 Minecraft
  自己的邊界處理攔下來重排，文字就疊在一起。現在最寬佔畫面 38%，超過就折行——
  折行交給 `Font.split`，它認得樣式與字型，每一段都保留原本的顏色。
- **看著戰鬥假人時譯文框一直閃。** 假人的字每被打一下就變（傷害會浮動），
  查不到譯文就把框收掉，於是一直閃。還在看同一個東西時維持上一次的譯文，
  直到玩家真的看向別處。

## [1.27.0] - 2026-08-21

### 新增

- **任務對話 3,494 句，取自 Wynncraft wiki。** 先前要有人真的把 158 個任務
  全部跑一遍才收得到，而且收到的是一堆看不出關聯的句子。wiki 把對話寫成
  結構化模板，**顏色、說話的人、台詞**三樣都在，還照階段排好。
  - 鍵是「任務#序號」，同一段劇情天然排在一起。
  - wiki 是人寫的，跟遊戲裡不保證逐字相同——定位是「先把量做起來、
    讓譯者看得到上下文」，遊戲內收集仍然是權威來源。
  - `tools/fetch-quest-dialogue.py`
- **加入翻譯團隊的說明。** 動手前先連絡 LyuChaCha（避免各翻各的），
  以及需要具備的能力：看得懂英文、能用中文表達，不用精通、不用會寫程式。

### 修正

- **`tools/build.py` 一直沒有載入地名。** 它只看 `fetch.py` 抓下來的原始檔，
  那個檔案不在就<b>整份地名清單都是空的</b>——語料裡沒有 `{p}`、遊戲裡卻有，
  含地名的條目全部查不到，而且從畫面上完全看不出原因。137 個地名現在都在了。
- **間隔補償把字黏在一起。** 中文標籤比英文長時補償是負的，技能樹的
  `Fire Damage: +15%` 就變成「火屬性傷害百分比+15%」，甚至被推出框外。
  原本是正的間隔現在有下限；原本就是負的照原樣通過——那是 Wynncraft 自己的
  排版設計，夾掉會讓坐騎的屬性列整排跑掉。

## [1.26.0] - 2026-08-21

### 新增

- **技能名稱只要翻一次。** 技能說明裡到處都在提別的技能
  （`Bash will hit a second time...`），先前得一條一條翻，翻完還是會不一致。
  現在把 `ability.json` 裡 `Bash` 那一條的名稱翻好，<b>所有</b>提到它的敘述
  自動換成譯名。
  - 用<b>名稱本身</b>當參照，不是座標代號。技能樹的位置會隨版本改動，
    而且譯者在語料裡看到 `Bash` 一眼就懂，看到 `warrior-1-1-5` 只能去查表。
  - 語料完全不動：模板不變、鍵不變，既有譯文照舊。
  - 只收技能與 Major ID 的名稱；裝備名稱不收，那些不會出現在別人的敘述裡。
    四個字元以下、小寫、夾在別的字裡面的都不換。
- **`{~1}` `{~2}`：指名要原文的第幾個數值。** 一句裡有兩個以上的數值時，
  中文語序常常跟英文相反——`+{~} to {~}` 想翻成「最多 {~2}，最少 {~1}」。
  沒有編號的維持照順序，兩種可以混用。
- **對話收集記下說話的是誰。** 對話事件本身不帶 NPC 名稱，改用玩家正前方
  最近的那個名牌。合併後的分段標題會寫成 `_── 任務名 · NPC ──`。
  - 是猜測不是事實，旁邊站著別的 NPC 就可能記錯，所以它只進 `ctx`，
    永遠不影響譯文本身。

### 修正

- **Major ID 名稱的顏色還是沒回來。** 名稱前面的 `✦` 在重建時是<b>獨立的一個
  token</b>，連著它一起比對永遠對不上。改成剝掉前綴再比對。

## [1.25.1] - 2026-08-21

### 修正

- **整段查表把段落之間的空行吃掉了。** 攤平查表會把空行壓成一個空格，於是整段
  連著上面的空行一起被換掉——譯文出來就跟上一段黏在一起。空行現在不併進來。
- **未鑑定物品的說明終於判得出置中。** 判斷邏輯本來就是對的：拿玩家回報的
  真實數字（縮排 0 / 4 / 49，內容 170 / 159 / 60）跑，那三行就是置中。
  問題出在<b>量出來的寬度</b>——那幾行有兩行用 Wynncraft 自己的字型
  （`language/wynncraft`），量出來跟預設字型差得夠多，光比像素就對不上。
  - 改成主要看「越長的行縮排越小」。這件事<b>不受量測誤差影響</b>：
    就算整塊都量偏了，長短的順序還是對的。
  - 像素上的容差改用<b>比例</b>（整塊寬度的 15%）而不是固定值——誤差會隨字型
    與行長放大，固定幾像素在長行上太嚴、短行上又太鬆。
- **折出來的行數比原本多。** 目標寬度是量原文量出來的，量不準就會多折幾行、
  整塊往下長。行數是看得出來對不對的：原本四行，折出來就不該超過四行，
  超過就把寬度放寬一成再試。

### 診斷

- 版面判斷同時印到 `latest.log`。檔案寫得出來與否受權限、路徑、防毒影響，
  而遊戲紀錄一定在——先前<b>連續三次</b>回報回來都是空的。

## [1.25.0] - 2026-08-21

### 新增

- **法術消耗分實數與百分比。** `Ice Snake Cost` 同時會出現 `-1 to -4`（實數）
  與 `-4 to -18%`（百分比）。五個職業各四個法術、加上第一到第四法術，
  共 24 組全部補上三種寫法。

### 修正

- **Major ID 的譯文斷行斷在莫名其妙的地方。** 折行時碰到寬度上限會退回上一個
  空白——那是為了不把<b>英文單字</b>切成兩半，但中文可以在任何字之間斷，
  退回去只會讓整行提早結束。「✦ 利他主義: 16」之後就換行、剩下的擠成三行，
  就是這樣來的。現在只有真的會切開英文單字時才退。
- **Major ID 的名稱掉了顏色。** 名稱在原文裡有自己的顏色，而譯文是中文、
  跟原文對不起來，一般的樣式沿用比對不到。「名稱：說明」這條路知道名稱譯成
  什麼，直接把它當成一個「原樣出現的詞」，樣式就跟著回來了。

### 診斷

- **置中判斷改寫到自己的檔案 `layout-debug.txt`。** 先前掛在 `line-debug.txt` 上，
  <b>連續三次</b>回報回來都是空的——共用緩衝區、共用去重、共用額度，
  任何一環出問題都會讓它靜默消失，而且從外面看不出是哪一環。
  現在自己的檔、自己的額度、不看任何開關，整段包在 try 裡。

## [1.24.2] - 2026-08-21

### 修正

- **Major ID 的說明還是英文。** 名稱前面的 `✦` 沒有被剝掉——它是<b>一般字元</b>
  不是材質包圖示，所以不會被抽成 `{#}`，於是拆出來的「✦ Altruism」永遠對不上
  語料裡的「Altruism」。行首的裝飾符號（`✦ ✔ ★` 這類）現在會跟 `{#}` 與空白
  一起剝掉再查，查到之後原樣接回去。
  - 這條路上有<b>四個</b>地方都會讓它靜默失效，四個都真的發生過。
    加了 `MajorIdTest`，用<b>真正的</b>語料跑一次遊戲畫面上的樣子——
    自己捏假資料只會證明捏的人想的是對的。
- **範圍值被從中間拆開。** `Earth Main Attack Damage -2414 to -1300` 變成
  `-2414 to` 後面一個大洞再接 `-1300`。從行尾往回找數值區時停在了 `to`，
  於是數值區只剩 `-1300`，補償就灌進 `to` 後面。`to` 現在算數值區的一部分。
  - 只認整段剛好是 `to` 的。`to sell (` 那種後面還接著別的，不算。

### 診斷

- 版面判斷不再需要先打開「收集語料」。那是<b>繪製</b>診斷不是語料收集，
  想回報版面問題的人沒有理由得先打開收集——上一份回報就是這樣空的。
- `line-debug.txt` 開頭一併記下收集開關的狀態。

## [1.24.1] - 2026-08-21

### 修正

- **Major ID 的說明還是英文。** 上一版做的攤平查表根本沒有被用到，兩個原因：
  - 攤平索引只收「正規化之後有變」的條目。但語料裡的長句<b>本來就沒有換行</b>
    ——會斷行的是畫面，不是語料。等於該收的一條都沒收。
  - 空白正規化寫成了 `"\s+"`。Java 15 起 `\s` 是「一個空格」的跳脫字元，
    所以那個式子只壓縮空格、<b>不含換行</b>，而換行正是要處理的東西。
  - 併幾行來試是看 `maxBlockLines`，那個值是從「鍵裡有幾個換行」算的，
    對這種語料裡只有一行的條目一無所知——說明在畫面上佔四行，卻從來沒有
    被併起來試過。有攤平索引時改成掃到八行。
- **技能樹的百分比詞條沒有用百分比的譯法。** 技能樹的標籤帶冒號
  （`Earth Damage:`），先前是拿整個模板去接 `%`，變成 `Earth Damage:%`——
  那個鍵不存在，於是退回一般的鍵。要先剝掉冒號再接 `%`，兩件事得疊在一起。
- **置中判斷改用一致性檢查。** 先前拿「最寬的那行」當基準，只要有一行的寬度
  量得不準，整塊都會被帶歪。改成看每一行的「內容 + 兩側留白」是否一致——
  置中的行本來就該算出同一個值，不需要挑基準。

### 診斷

- 版面判斷有自己的額度。先前跟逐行診斷共用同一個上限，滑過幾個 tooltip
  就被佔滿，真正要看的那一份反而一筆都沒寫到。

## [1.24.0] - 2026-08-21

### 新增

- **依畫面寬度斷行的長敘述終於查得到了。** Major ID 的說明在遊戲裡是
  <b>依 tooltip 寬度自動斷行</b>的，斷點取決於玩家的畫面設定；語料存的是
  沒斷行的完整句子，兩者永遠不會逐字相等。現在整段查不到時，會把幾行攤平
  成一行、空白正規化之後再查一次，然後照原本那幾行的寬度折回去。
  - Major ID 是「名稱: 說明」擠在同一行，而語料裡是<b>兩筆</b>。
    所以也會從冒號拆成兩半各查一次——冒號兩邊都得剛好在語料裡才算數。
- **`tools/sync-credits.py`：README 的名單由 `credits.json` 產生。**
  同一份名單維護兩份的結果就是分岔，實際發生過。

### 修正

- **`ui-labels.json`：實數與百分比補齊。** `Main Attack Damage` 在同一件裝備上
  同時出現 `+65%` 與 `+334`，先前只有一種譯法。判斷依據回到遊戲自己的訊號：
  看得到 `X Raw` 這個標籤，就代表兩種都有——23 組全部補上三種寫法。
- **`major-id.json` 的數字沒有參數化。** 語料存的是 `Allies within 16 blocks`，
  遊戲裡的模板是 `Allies within {~} blocks`，差一個數字就查不到。115 條已修正。
- **F6 的 NPC 名牌設定，說明文字標錯欄位。** 說明畫在自己欄位的<b>下方</b>，
  讀起來卻像是<b>下一個</b>欄位的標題，最後一行還掉出面板外面。
  改成畫在所標示的輸入框上方。

### 診斷

- `line-debug.txt` 開頭記下模組版本，並加上「版面判斷」段落：每一行的縮排、
  內容寬度、置中應有的縮排。置中判斷失敗時，畫面上只看得到「沒有跟著置中」，
  分不出是分段切錯、寬度量錯、還是算式差了幾像素——三種的修法完全不同。

## [1.23.1] - 2026-08-21

### 修正

- **`ui-labels.json` 整理過一輪。** 百分比一律寫成「…百分比」（`火屬性傷害百分比`），
  不再用 `%` 符號。
  - 移除 `Critical Damage%`、`Health%`、`Main Attack Damage%`、`Spell Damage%`——
    這幾個沒有確認過實數與百分比並存，留著只會讓後面的人以為遊戲裡真的有兩種。
  - 確認兩種都有的只有：`Health Regen`、五系的 `X Damage` / `X Main Attack Damage` /
    `X Spell Damage`、`Elemental Damage`、`Elemental Spell Damage`、`Neutral Damage`。
  - `XP Bonus` 改回對照表寫的「經驗加成」。
- **檔案照用途分好類。** 加了底線開頭的分段標題（`_── 詞條 · 火屬性 ──`），
  同一個詞條的實數／百分比／`Raw` 三種寫法排在一起，一眼就看得出少了哪一個。
  底線開頭的鍵程式會跳過，只是給人看的。

## [1.23.0] - 2026-08-21

### 修正

- **實數詞條顯示成百分比。** `Thunder Damage: +1-8` 顯示成「雷屬性傷害百分比」——
  遊戲裡實數與百分比用的是<b>同一個英文標籤</b>，所以那個鍵不能寫死成百分比。
  23 組詞條統一成三種寫法，物品 tooltip 與技能樹一起適用，不必分開維護：

  | 英文標籤 | 中文 |
  |---|---|
  | `Fire Damage` | 火屬性傷害 |
  | `Fire Damage%` | 火屬性傷害% |
  | `Fire Damage Raw` | 火屬性傷害值 |

- **譯文不再被迫沿用原文的換行**（[#44](https://github.com/LyuChaCha/WynnChaYuan/issues/44)）。
  中文比英文緊湊，原文分兩行的句子往往一行就講完，硬湊行數會斷在莫名其妙的地方。
  佔位符改成<b>整段照順序</b>取用，所以 `{~}` 搬到上一行或下一行都可以，
  只要整段的數量對得上。
- **貢獻者一多，關於頁就壓到頁尾與按鈕。** 名單改成自動分欄，
  先算得下幾欄再排；欄數越少越好讀，所以由少到多試。
- **突襲選完增益之後，譯文框還飄在畫面上。** 「移開視線後再顯示一下」是為了
  不讓框在準心晃開時閃掉，但那段字的<b>來源消失</b>時不該再停留。
  現在會記住來源，來源不在就立刻收掉。

### 文件

- Modrinth 說明改成英文在前、中文在後。Modrinth 的內容規則要求
  Summary 與 Description 必須有英文版。

## [1.22.0] - 2026-08-20

### 新增

- **原文裡帶樣式的詞，譯文照樣帶著。** `Reduce the Mana cost of Bash.` 裡的
  `Bash` 原本有底線與專屬顏色，譯文出來卻是一片平的。技能名這類詞照慣例
  保持英文、原樣出現在譯文裡，比對得到就把原本的樣式搬回去。
  - 搬的是樣式<b>物件</b>而不是顏色碼，所以非原版的自訂顏色也對——
    `§` 格式碼只表達得出十六個原版顏色。
  - 譯者什麼都不用寫。`ability.json` 的 592 條描述裡有 402 條句子中夾著
    別的技能名稱，全部立刻受惠，包含已經翻好的。
  - 對不上就照舊（那個詞被翻成中文了），不會比現在更糟。

### 修正

- **未鑑定物品的說明還是沒有跟著置中。** 上一版修了「分隔線是符號組成的」，
  但這裡的問題是<b>根本沒有分隔線</b>：物品名稱、稀有度跟後面那三行中間
  什麼都沒有，五行一起判斷時前兩行對不上算式，於是一行都沒調整。
  現在整段對不上時會再找段內最長的一串吻合的行，要求至少三行、
  而且其中要有明顯的縮排。
- **整行的主樣式取錯。** 先前取「第一段文字」的樣式，句子以帶樣式的詞開頭時
  整行都會被畫上底線。改成取涵蓋字數最多的那一個。
- **`Elemental Spell Damage`、`Health Regen` 的實數顯示成百分比。**
  這兩個標籤的實數（`+372`、`-1050`）走的是不帶 `%` 的鍵，
  所以那個鍵要是實數的講法，百分比交給 `X%`。

## [1.21.1] - 2026-08-20

### 修正

- **關掉「翻譯物品名稱」時，技能名稱也被一起關掉了**（[#36](https://github.com/LyuChaCha/WynnChaYuan/issues/36)）。
  那個開關是為了讓裝備名稱保持英文、對得上 wiki 與交易市場；技能名稱跟交易市場
  毫無關係，不該受它影響。先前的判斷是「只要 `role` 是 `name` 就算物品名稱」。
  - 改成由譯文檔自己宣告：`_meta.itemNames`。沒寫的當物品——漏標一個裝備檔會讓
    開關<b>無聲失效</b>，漏標一個技能檔頂多是名稱跟著關掉，後者看得出來。
  - `ability.json`、`major-id.json` 已標為 `false`。

## [1.21.0] - 2026-08-20

### 新增

- **浮在世界裡的字改放 `label.json`。** 突襲裡選增益的那幾塊、地上的指示與提示，
  用的都是跟 NPC 名牌同一種實作，於是全部混進了 `npc.json`——譯者要在幾百個
  NPC 名字之間翻找，等於找不到。Wynntils 沒認成 NPC 的另外放一個檔。
  - 只影響譯者在哪裡找得到，遊戲端查表不分檔案，既有譯文照舊生效。

## [1.20.0] - 2026-08-20

### 修正

- **`ability.json` 翻了卻沒反應。** 技能說明的原文本來就是一整段，
  官方 CDN 給的是含換行的一大串；遊戲畫面上是三、四行，但鍵是那幾行黏在
  一起的樣子。逐行查表永遠查不到，而且失敗得無聲無息。現在會先把連續幾行
  併起來查<b>整段</b>，查不到才退回逐行。
  - 由長到短試，讓涵蓋比較多行的條目優先。
  - 譯文行數必須與原文一致（`validate.py` 本來就會擋），對不上的整段跳過。
- **未鑑定物品的置中說明沒有跟著調整。** tooltip 的分段線不是空行，
  而是<b>由排版符號組成的一行</b>，於是整份 tooltip 被當成同一段，
  置中的三行跟物品名稱、稀有度擠在一起判斷，一行都沒有被調整。
  改成看「剝掉符號之後還有沒有字」。
- **裝備敘述裡的型號被當成數值。** `A16-L31 Handheld Mortar` 被抽成
  `A{~}-L{~}`，譯者看到的是兩個沒有意義的佔位符，完全不知道那句話在講什麼。
  前面緊接著字母的數字不再參數化，已修正 6 筆。
- **移除比對不到的百分比詞條。** 近戰反傷、遠程反傷本來就只有百分比，
  魔力回復、生命竊取、中毒本來就只有實數，替它們多開一個 `%` 版本只會
  讓人以為遊戲裡真的有兩種。另外 `"Spell Damage %"` 那種<b>中間有空格</b>的
  寫法永遠比對不到——機制是直接在標籤後面接 `%`。兩類一併拿掉。
  - 遊戲本身多半是用 `Raw` 後綴區分的（`Spell Damage` 是百分比、
    `Spell Damage Raw` 是實數），那些鍵照舊，不受影響。

### 新增

- **任務對話照任務分堆。** 收集時一併記下當下追蹤的任務，合併時同一段劇情的
  台詞會排在一起，並插入 `_── 任務名 ──` 分隔列。底線開頭的鍵不會被載入，
  只是給譯者看的段落標題。
  - 依據是收集當下追蹤器上的任務，不保證百分之百對應；分錯了只影響排序。
- **擋掉更多玩家內容。** 玩家自製裝備的製作者署名（`by <玩家 ID>`）
  與經驗共享通知後面掛的那個人，先前會混進共享語料，已一併清掉既有的 5 筆。

## [1.19.0] - 2026-08-20

### 新增

- **同一個標籤可以分「實數」與「百分比」兩種譯法。** `Health Regen -605` 與
  `Health Regen -162%` 的英文標籤一模一樣，中文常常要分開講。
  在鍵的尾端加 `%` 就只會用在數值是百分比的時候：
  `"Health Regen%": "生命回復百分比"`。沒加的標籤完全不受影響。
  - 判斷看的是<b>數值本身</b>，不是整行有沒有 `%`——`+282 [53.8%]` 後面那個
    是詞條品質，不是數值。
  - 已補上 16 組常見的百分比版本。
- **`tools/order-abilities.py`：技能樹照實際版面排序。** 官方 CDN 的每個節點
  都帶 `location: {page, row, col}`，那就是它在技能樹畫面上的位置。
  照「職業 → 頁 → 列 → 欄」排，就是遊戲裡由上而下、由左而右的順序。
  - 1,240 條全部定位到，不需要在遊戲裡重新收集。

## [1.18.0] - 2026-08-20

### 修正

- **Major ID 翻了卻沒生效。** 遊戲裡顯示的是 `Altruism:`（帶冒號），
  語料的鍵是 `Altruism`，只差一個冒號就查不到。查不到時會先剝掉尾端的
  冒號再查一次，冒號原樣接回去。
  - 說明的部分仍然對不上：那段文字會依 tooltip 寬度<b>自動斷行</b>，
    斷在哪裡取決於玩家的畫面設定，我們的比對是逐行進行的。

### 新增

- **`{u}` 佔位符：玩家自己的名字。** 任務對話會直接叫玩家的名字，先前那些行
  一律<b>整行丟掉</b>——連帶丟了一大批對話。現在參數化，一條譯文所有人通用，
  名字也不會寫進共享檔案。
- **`captured.json` 記錄收集順序（`seq`）。** 條目是用雜湊當鍵的，順序等於隨機；
  對任務對話來說這是致命的——同一段對話的十幾句會散在檔案各處，
  譯者看不出上下文，也分不清誰在回答誰。
  - `merge_captured.py` 對話照收集順序排，其餘照出現次數排。
- **`tools/sort-corpus.py`：把語料按來源分組。** 技能說明先前五個職業混在一起，
  現在照 `職業/技能` 排好，可以「一棵樹一棵樹翻」。

## [1.17.7] - 2026-08-20

### 修正

- 標籤很長、中間只剩一個空格的詞條沒有對齊（`Elemental Spell Damage +372`）。
  用「數值帶正負號」把它跟 `Emerald Pouch [Tier 8]` 那種標籤分開。
- 貢獻者標記與 Wynntils 的 donator 標記重疊，後畫的會蓋掉先畫的。標記往上挪一行。

### 調整

- 「與物品的間距」改成自由輸入像素（0–200），不再是固定的幾段。

## [1.17.6] - 2026-08-20

### 修正

- **坐騎屬性整排右移（八行一起）。** 我在 v1.13.0 加了「欄位間隔不該是負的」
  的夾限，但<b>Wynncraft 自己就在用負間隔</b>——坐騎的屬性列原文就是
  「-25px 再 +104px」這種寫法，夾成 0 之後整行多出 20 幾像素。
  - 診斷檔寫得很清楚：`空白 -25 px -> 0 px (對齊欄)`。夾限已移除。
- **`Class Type` 那一行完全沒被補償。** 它的間隔是<b>字面空格</b>、數值又是
  <b>文字</b>（`Mage/Dark Wizard` 沒有數字），兩個條件都躲過了原本的判斷，
  一路回傳「找不到交界」。
  - 補上第三種判斷：一整段都是空白、前後都有內容、而且至少兩格。
    一格是詞距，不是欄位。
- **貢獻者標記還是不出現（第三次）。** 認人改成<b>先問實體本身</b>：
  用 render state 的實體 id 查出玩家帳號名稱。
  - 名牌上的文字不可靠——Wynntils 把它整個換掉了（階級前綴、donator 標記、
    公會標籤都是它加的），字面比對認不出來。帳號名稱則是事實，誰都改不了。
  - 查不到才退回比對名牌文字。

### 隱私

- 收集端補上三道濾網，都是從實際回報的 `captured.json` 裡看到才加的：
  - **出現中文就擋**。Wynncraft 的原文全部是英文，收到中文只有兩種可能：
    玩家自己打的字（攤位招牌、名牌留言），或<b>我們自己的譯文繞回來了</b>
    ——就地取代模式下畫面上已經是中文，再收集一次就會把譯文當成原文。
  - `left the game`（沒有 `has` 的版本）也夾帶玩家名。

### 新增

- 併入實際跑任務收集到的 **330 條**：162 條 NPC 名牌、168 條任務對話與追蹤目標。
  座標已正確參數化成 `{~}`，所有玩家共用同一條譯文。

## [1.17.5] - 2026-08-20

### 新增

- **任務追蹤框現在會出現了。** 它的規則是「一行都沒翻到就不畫」——而追蹤器的
  字串<b>一條都不在語料裡</b>，所以永遠不會出現。那不是程式問題，是沒有資料。
  - 補上追蹤目標的動詞開頭（擊殺／採集／攜帶／摧毀／探索…）。那些在畫面上是
    <b>獨立的樣式片段</b>（動詞白色、目標高亮），所以翻了就能讓框出現，
    即使任務名稱還沒人翻。
  - 任務名稱翻好之後會更完整——157 個名稱已經在 `quest.json` 等人填。

## [1.17.4] - 2026-08-20

### 修正

- **貢獻者標記還是不出現。** 上一版改訂閱 Wynntils 的名牌事件是對的方向，
  但少了一個旗標：NeoForge 的匯流排<b>預設不把已取消的事件發給後面的監聽器</b>，
  而 Wynntils 正是<b>用取消來接管</b>名牌繪製的。
  - 所以在「有 donator 標記的玩家」身上，我們的監聽器一次都不會被呼叫
    ——也就是最需要它的那些人身上。加上 `receiveCanceled = true` 就好了。
  - 取消對我們沒有意義：那是 Wynntils 在說「原版不要畫」，不是「誰都不要畫」。

### 流程

- **分支保護的「需要幾人核准」改成 0**，必要的狀態檢查維持不變。
  - 原本要 1 人核准，但 GitHub <b>不允許自己核准自己的 PR</b>——維護者自己送的
    PR 永遠卡住，只能按 bypass。而一旦習慣按 bypass，紅燈的 PR 也會被按下去，
    已經因此混進兩批壞掉的譯文。
  - 現在「綠燈就能合併、紅燈就是不能合併」，那條線比「有沒有人按核准」重要得多。
- `CONTRIBUTING.md` 補上「已經 fork 了但不知道下一步」與維護者的合併流程。
  目前貢獻者都停在 fork 那一步，沒有人開 PR——那一步在介面上很容易漏掉。

## [1.17.3] - 2026-08-20

### 修正

- **接在標籤後面的欄位間隔沒有被補償。** 上一版把那些偏移字元保住了，
  但它們包在「文字片段」裡，補償程式碰不到——譯文變短時數值仍然往左跑
  （診斷檔顯示 `Durability󏿗󐂑-30` 整行差 20px）。
  - 現在會把尾端的<b>純寬度偏移</b>拆成獨立的空白片段。那本來就是欄位間隔，
    只是沒有獨立成一段。
- **貢獻者標記完全不會出現。** 原本用 mixin 掛在原版名牌繪製的尾端，
  但 <b>Wynntils 接管了玩家名牌</b>——它的 `CustomNametagRendererFeature`
  會取消掉原版那條路、自己畫（donator 標記就是這樣來的），所以尾端根本走不到。
  - 改成訂閱 Wynntils 的 `PlayerNametagRenderEvent`。事件無論如何都會發，
    被不被取消是它的功能在決定的。
  - 這也把整個模組<b>唯一的 mixin</b> 拿掉了。

### 新增

- **`major-id.json`：161 種 Major ID**，由 `tools/fetch-majorids.py` 從官方 CDN
  匯入，不必進遊戲也不必翻 wiki。
  - 名稱與說明分開存：遊戲裡的說明會依畫面寬度自動斷行，整段多半對不上，
    但<b>名稱</b>是獨立的一段，一定對得上。說明照樣收進來，譯者翻名稱時
    看得到上下文才知道那個技能在做什麼。

### 流程

- **fork 的 PR 現在也會自動跑檢查。** 先前用 `pull_request`，那對「第一次貢獻
  的人」需要維護者手動按 Approve and run——實際發生的是沒人按、檢查沒跑、
  PR 就被合併了，已經因此混進<b>兩批</b>壞掉的譯文。
  - 譯文改用 `pull_request_target`（不需核准）。它拿的是有寫入權的 token，
    所以只 checkout <b>本倉庫</b>的 `tools/`，PR 那邊<b>只取譯文 JSON 當資料</b>
    ——JSON 不會被執行。動到 `tools/` 的 PR 仍走一般 `pull_request`，
    那是程式碼，本來就該人工核准。
- 修正 PR #9 帶進來的 6 筆技能翻譯（換行數與佔位符不符，其中 3 筆是把先前
  修好的又改回去了）。

### 調整

- 對照表的 `Loot Bonus` 跟上實際譯文（掉落加成 → 寶物加成）。
  這是譯者改了譯文但沒改對照表，CI 抓出來的。

## [1.17.2] - 2026-08-20

### 修正

- **素材 tooltip 的欄位間隔整個消失**（`耐久度-160`、`持續時間-150s`、
  配方清單擠在一起）。診斷檔一次把根因照出來了：

  ```
  原文: ...text=Jeweling<U+D0004><U+D0034>
  譯文: ...text= 珠寶            ← 兩個偏移字元不見了
  ```

  排版偏移<b>黏在文字片段裡面</b>（名稱與寬度同一段），而
  `LineParts` 對文字片段是直接 `stripGlyphChars` 丟掉那些字元——沒有變成
  `{#}` 佔位符，還原時自然也就填不回去。
  - 現在文字片段<b>頭尾</b>的偏移會抽成佔位符交給 glyphs 保管。
  - 只處理頭尾：夾在句子中間的圖示是內容的一部分，本來就不該進語料的鍵，
    而且抽出來會讓 `{#}` 卡在模板中間，既有的譯文全部對不上。

### 新增

- 開發者：**芋圓YuYuan**。
- 貢獻者：**泥巴先生、幻影Joker、Pootato、N02sAyLa、兔寶、Jimmy、雪花**。

## [1.17.1] - 2026-08-20

### 修正

- **加上「量出來」的收尾校正。** 欄位補償是逐片段<b>累加</b>算出來的，
  只要有任何一項沒算進去（片段間的字距、字型換掉造成的寬度差、編碼後空白
  字元的實際寬度），整行就會差幾像素到幾十像素，而且看不出是哪一段漏了。
  - 現在算完之後<b>再量一次整行</b>，殘差補回最後一個對齊欄。算對的話殘差
    是 0、什麼都不動；算漏了就在這裡補回來。這是自我修正，不是另一套演算法。
  - 這也正是這個檔案原本的設計註解寫過的原則——後來改成逐片段累加時漏掉了。

### 其他

- **診斷檔改成每一行都記。** 先前只記「有排版空白」的行，結果真正壞掉的那些
  ——間隔不是用空白字元做的——反而完全看不到。同一句仍然只記一次，
  所以不會被洗版；額度 25 → 40。
- 診斷檔多印一行「整行寬度 X → Y」。那是「這行對齊對不對」最直接的答案，
  逐片段的數字要自己加總才看得出來，加總錯了又很難察覺。

## [1.17.0] - 2026-08-20

### 修正

- **材料的配方清單還是被推成置中。** 上一版的判斷「同一塊裡的縮排不全相同
  就是置中」太寬鬆——材料 tooltip 裡 `4 Crafting Level` 的縮排剛好和配方
  清單不同，整塊就被誤判了。
  - 改用直接的算式：置中的行，縮排必然滿足
    `縮排 ≈ (整塊最寬 − 本行寬) / 2`。<b>每一行都吻合</b>才算置中；
    只要有一行對不上，那個縮排就是別的用途，整塊都不該動。
  - 另外要求縮排<b>真的有變化</b>。縮排全都一樣時，「置中」與「靠左」畫出來
    一模一樣，當靠左最安全——這條也擋掉窄區塊的誤判（兩行只差十幾像素時，
    光靠容差會讓「縮排都是 0」剛好符合算式）。

## [1.16.0] - 2026-08-20

### 修正

- **材料的配方清單被推成置中。** 上一版讓 0 像素的前導偏移被認出來，
  未鑑定物品的置中因此修好了——但<b>靠左的行也一起吃到置中補償</b>。
  - 置中與靠左，在<b>單獨一行</b>上長得一模一樣：都是「一段前導偏移，
    後面接文字」。未鑑定物品第一行是「偏移 0」，配方清單第一行也是「偏移 0」，
    可是正確處理完全相反。
  - 判斷改成<b>看整塊</b>：置中的行前導偏移必須逐行不同（那正是它對齊中線的
    方式），靠左的行則整塊共用同一個縮排。段落以空行分隔。

### 新增

- 精通書卷、職業資訊、公會階級、寶箱優惠、採集／製作技能等 30 條介面字串。

## [1.15.0] - 2026-08-20

### 修正

- **未鑑定物品的說明沒有置中，配方那種「一行兩欄」的第二欄也沒對齊。**
  兩者是同一個判斷的兩個漏洞——先前用「編解碼繞得回來」來認排版空白：
  - **0 像素的偏移永遠判定失敗**，因為 `encode(0)` 回傳空字串。
    未鑑定物品第一行的前導偏移剛好是 0，於是整個空白沒被認出來，
    置中補償完全沒跑。
  - **多個字元組成的偏移也失敗**：`decode` 會加總，`encode` 只吐一個字元，
    字串當然對不起來。配方的欄位間隔就是多字元的。
  - 改成看<b>每個碼位是不是都落在偏移範圍內</b>。字型已經是
    `minecraft:space`，範圍內的碼位依定義就是寬度偏移。

### 隱私

- **名牌收集現在也會過濾玩家資料。** 玩家攤位的名牌<b>整塊都是玩家內容</b>
  ——名稱是 ID，下面是他自己打的招牌字。先前只有聊天走這道濾網，
  於是 `captured.json` 裡混了一堆別人的攤位。

### 新增

- 交易市場篩選、地城鑰匙、等級篩選、分頁等 23 條介面字串。
- 銀行、傳送點、住宅管理員、粉末大師、隊伍尋找器等 26 條 NPC 名稱與說明。

## [1.14.0] - 2026-08-20

### 修正

- **標籤被翻譯的那些行，欄位間隔整個消失**（`耐久度-125`、`持續時間-300s`）。
  - 根因在 `translateOneSegment`：它把重建好的 Component <b>壓成純字串</b>
    再回傳，丟掉了各片段的字型；呼叫端接著整段套上預設字型。
  - Wynncraft 的排版偏移字元<b>只有在自己的字型底下才有寬度</b>。
    被改成預設字型之後就變成沒有寬度的未指派碼位——間隔於是憑空消失。
  - 這解釋了為什麼只有「標籤有被翻譯」的行會黏住：沒翻到的片段
    原封不動，字型自然還在。
  - 片段現在整段以 {@code Component} 保存，圖示與偏移各自保留原字型；
    量原文寬度也改用<b>原本的字型</b>——先前用譯文的字型去量原文，
    算出來的差額本來就是錯的。

### 新增

- Scribing（抄寫）、無法交易的物品、綜合手冊、角色資訊、任務／探索點統計等
  20 條，取自回報的 `tooltip-debug`。

### 其他

- **診斷檔不再被同一行洗版。** tooltip 每幀重畫，滑鼠停兩秒就寫滿 25 筆
  一模一樣的內容——前兩次收到的診斷檔就是這樣，真正要看的裝備行一筆都沒有。
  現在同一句只記一次。

## [1.13.0] - 2026-08-20

### 修正

- **對齊空白會被整個刪掉，數值突然黏到標籤旁邊。** 兩個相關的錯：
  - `SpaceOffset` 只認<b>正</b>偏移。但 Wynncraft 也用負的——實際 tooltip 裡
    出現過 `U+CFFE7`（比基準低 25，往回 25 像素）。那些字元既解不出寬度，
    重新編碼又變成空字串。
  - `encode` 對 `<= 0` 一律回傳空字串，等於<b>把整個對齊空白刪掉</b>。
    畫面上就是數值黏死在標籤旁，而且完全看不出是被刪掉的。
  - 現在負偏移解得出也編得回；欄位間隔則夾在 0 以上——譯文比原文寬時
    正確的結果是「貼齊」，不是「刪掉整個間隔」。

### 新增

- 消耗品與坐騎的欄位：持續時間、充能、作用範圍、速度、加速、高度、能量、
  操控、堅韌、衝刺、訓練、右鍵點擊召喚等。
- 住宅與公會島嶼選單的 12 條字串（取自回報的 `tooltip-debug`）。

### 其他

- `line-debug.txt` 只記錄<b>有排版空白</b>的行。上一版的額度被住宅選單的
  純文字行佔滿，反而沒抓到真正要看的裝備行。額度也從 12 提高到 25。

## [1.12.0] - 2026-08-20

### 修正

- **最後一欄的右緣沒對齊。** 補償只把「空白之前」的寬度變化補掉，所以數值的
  <b>起點</b>會落回原位——但數值本身如果也被翻譯了
  （`Mage/Dark Wizard` → `法師/黑巫師`），它的<b>結尾</b>就短了一截。
  Wynncraft 是把數值靠右排的，現在把剩下的差額一併加回最後一個對齊空白。

### 新增

- **`wynntils.json`：Wynntils 綜合頁面（內容書）的翻譯。** 距離、長度、難度、
  獎勵、追蹤提示等。放的是<b>片段</b>而不是整行——那些欄位的標籤與數值是
  分開的樣式片段，分開比對比整行模板穩得多。
- **`tools/fetch-quests.py`：從 wiki 抓任務清單。** 157 個任務名稱已補進
  `quest.json`，**不需要有人把每個任務都跑一次**。
- **跨檔重複鍵的檢查。** 同一個原文出現在兩個檔案又譯法不同時，
  載入順序決定誰生效——改到輸的那一份會覺得「明明改了卻沒反應」，
  而且畫面上完全看不出來。
  - 加上去當下就抓到三組：`Discovery`、`Combat Lv. Min:`、`Medium`。
- `line-debug.txt` 現在也記錄逐片段那條路的中間狀態（每個空白補償前後的像素值）。
  「間隔完全消失」與「補太少」在畫面上都是歪掉，但成因不同，看數字才分得出來。

### 調整

- `Discovery` 統一譯為「探索點」（原本 `quest.json` 是「發現」）。
  那是地圖上的收集點，「發現」當名詞在中文裡看不出是個東西。

## [1.11.0] - 2026-08-20

### 調整

- **對話框與名牌改成淡出，不再啪一聲消失。** 那兩個框就在視線中央附近，
  時間一到瞬間不見，眼角餘光會以為畫面閃了一下。淡出 350 毫秒就沒有那種突兀感。
  - **換內容時不淡。** NPC 講下一句時計時重新開始，透明度直接回到全滿——
    那是「換內容」不是「結束」，中間插一段淡出反而像卡頓。
  - 這件事不需要額外的狀態：透明度是從「距離上次更新多久」算出來的，
    更新時間一被推後，透明度自然就回到 1。
  - 底色、邊線與文字一起淡。只淡其中一個的話，收尾那幾幀會看到一個
    沒有內容的空框，比直接消失還怪。

## [1.10.0] - 2026-08-20

### 修正

- **`Emerald Pouch [Tier 8]` 這種行被推歪。** `[Tier 8]` 只是接在名稱後面
  <b>一個空格</b>，不是右對齊的欄位；當成欄位補償的話，譯文變短就把它
  一路推到原文的右緣去。
  - 現在用「間隔有幾格」來分：一格是詞距，兩格以上才是欄位。
    已經是排版空白字元的情況不受影響——那種本來就是欄位。
- **`Elemental Defences` 沒翻到。** 語料裡只有單數的 `Elemental Defence`，
  遊戲裡是複數。兩種寫法現在都吃得到。
- **對話框與名牌擺在正中央，實際跳出來卻偏左。** 那兩個框的寬度隨內容變動，
  存的卻是左緣——短句自然就偏左。現在存<b>水平中心</b>。
  （翻譯面板與任務追蹤是左對齊的清單，仍然存左緣。）

### 新增

- **[GLOSSARY.md](GLOSSARY.md)：專有名詞對照表。** 翻譯時碰到不確定的詞就查，
  不要自己另外想一個——同一個詞兩種譯法，玩家會以為是兩個不同的東西。
  - 收錄照字面翻會翻錯的：`Reflection` 是「遠程反傷」不是「反射」，
    `Thorns` 是「近戰反傷」不是「荊棘」，`Scale` 是「詞綴」不是「規模」。
  - `tools/validate.py` 會檢查譯文與對照表是否一致，不一致就在 PR 上警告。
    只有一份檔案，不會出現「表與實際譯文說法不同」的情況。
- 貢獻者：**SCPNightsky**（`SCP_Night_sky`）。
- `line-debug.txt`：把「有翻到但版面怪怪的」那些行的片段結構寫出來
  （字型、空白像素、隱形碼位）。只在收集模式下寫，最多 12 行。
  已有的 `tooltip-debug` 負責「沒翻到」的行，兩者互補。

## [1.9.0] - 2026-08-20

### 新增

- **貢獻者標記**：名單上的人，名牌上方會多一行 `WynnChaYuan {身分}`。
  - 顏色依身分：開發者黃、贊助者水藍、貢獻者綠。
  - 身分不只一種的人<b>逐字漸層</b>掃過所有顏色；三種都有的給整段彩虹。
  - **只有裝了這個模組的人看得到**——這是純客戶端繪製，伺服器不知情，
    被標記的人自己也看不到（除非他也裝了）。
  - 開關與呈現方式在 **F6 →「關於／貢獻者」** 頁面底部。放在那裡是因為
    它就是「這份名單要不要顯示在遊戲裡」；塞進主設定頁的話，看到那個選項
    的人不會知道它在講誰。
  - 名單仍是 `credits.json`，加人送 PR 即可，不必改程式也不必重新發版。

### 實作說明

- 這是整個模組<b>唯一的 mixin</b>。名牌渲染沒有對應的事件，只能走這條路。
  - 不重寫任何渲染：在原版畫完之後，用同一支 `submitNameTag` 再送一次，
    只把附著點往上抬一行、文字換成標記。面向相機、深度、背景、距離淡出
    全部沿用原版行為。
  - 注入失敗時<b>安靜地不顯示</b>（`required: false`）。標記只是裝飾，
    不值得因為 Minecraft 改版就讓玩家的遊戲開不起來。
- 認人是拿名單裡的 ID 去名牌文字裡找，要求前後是詞的邊界——名牌上會有
  階級前綴與顏色，不能直接比對；用「包含」的話 `Bob` 會誤中 `Bobby`。

## [1.8.0] - 2026-08-20

### 修正

- **素材的兩欄數值行仍然對不齊**（標籤和第一個數值黏在一起，第二欄被推過頭）。
  - 找交界時會先短路回傳「整行第一個對齊空白」，但素材那種行在<b>標籤與第一個
    數值之間根本沒有空白</b>（padding 塞在標籤片段裡），只有兩個數值之間有。
    看到後面那個就以為不用補，差額於是全部灌進第二欄。
  - 改成同時看「現成空白」與「數值區起點」，取較前面的那個。
- **注視 NPC 常常不顯示翻譯。** 名牌浮在<b>頭頂</b>，玩家卻是對著<b>身體</b>看的；
  站在 NPC 面前兩格時那個高度差就是十幾度，遠超過預設 6 度的錐。
  - 改用「離視線這條直線多遠」判斷，並給一個不隨距離縮小的下限（1.4 格）。
    夾角設定繼續管遠處——遠了才需要「對準」的語意。
- 空白字型底下若掛著<b>不是</b>寬度偏移的字元（材質包自訂圖示），逐片段那條路
  會把它重新編碼成別的東西。整行重建那條路已有防護，現在兩邊一致。

### 新增

- **Modrinth 自動發佈**：建立 GitHub Release 時自動上傳。
  需要一次性設定（見 `.github/workflows/publish-modrinth.yml` 開頭的說明）。
- `tools/fetch-wynntils.sh`：從 Modrinth 抓對應版本的 Wynntils，CI 才建置得起來。
  版本從 `gradle.properties` 讀，升 Minecraft 版本時不會默默抓到舊的。

## [1.7.0] - 2026-08-20

### 修正

- **未鑑定物品的說明整排偏左。** 那三行在原文是各自置中的，置中靠的是
  行首的空白偏移——而那個偏移是按<b>英文的寬度</b>算好的。
  - 根因是這種行走的是「整行重建」那條路，而那條路<b>完全沒有做對齊補償</b>：
    空白原樣填回去，中文比英文短，整排就往左偏。
  - 現在整行重建的結果也會重新對齊：以空白為錨點（重建時是一對一的），
    行首空白補差額的一半（置中），其餘欄位交界補累計位移。
  - 順帶擋掉一個沒發生但會很難查的問題：`minecraft:space` 底下也可能掛著
    <b>不是</b>寬度偏移的字元。現在要求編解碼能原樣繞回來，繞不回來就
    原封不動抄過去，不會把材質包圖示重新編碼成別的東西。

### 新增

- **名牌的偵測距離與夾角可以自己調**（F6 →「NPC 名牌設定…」）。
  城裡 NPC 站得密就把角度調小，曠野找人可以調大——沒有一個值兩邊都好用。
- **準心真的指到誰就優先翻譯誰。** 先前只比夾角，但夾角會隨距離變小：
  遠處的 NPC 就算在畫面邊緣，夾角也可能比你面前這位還小，結果站在攤位前
  卻翻到後排的名字。
  - 現在先用射線打名牌的碰撞箱，命中的最近者優先；都沒命中才退回夾角錐，
    而且錐內<b>比距離</b>不比夾角。
- 名牌的模式、停留秒數、距離、夾角集中到獨立子頁，主設定頁不再擁擠。
- README 加上團隊名單（開發者／贊助者／貢獻者），與遊戲內同一份資料。

## [1.6.0] - 2026-08-19

### 修正

- **素材 tooltip 的數值欄位對不齊，而且看起來很隨機。** 素材那種行有
  <b>兩個</b>對齊欄（最小值一欄、最大值一欄），先前只補其中一個，另一欄就跑掉。
  更麻煩的是**整行總寬度仍然是對的**，所以量寬度的自我檢查完全看不出問題。
  - 改成由左往右逐欄補償：每碰到一個對齊欄就把累計的位移補回去，
    有幾欄補幾欄。譯文長度不同造成的位移不會再往後累積。
  - 「隨機」的成因是有的行只有一欄、有的兩欄，補對補錯就看運氣。
- **打怪時會冒出翻譯小框。** 傷害數字、`Dodged`、`Blocked` 這些浮動提示，
  在遊戲裡是<b>用名牌實作的</b>，事件層跟 NPC 名牌分不出來。
  - 根因是 `Dodged` 曾被當成 NPC 名稱收進 `npc.json`，於是傷害提示真的
    查得到譯文，框就跳出來了。該筆已移除。
  - 新增 `CombatText` 判斷，這類提示既不顯示也不收集——傷害數字每次都不一樣，
    收進 `captured.json` 只會累積成幾千條不會有人翻的垃圾。

### 新增

- **貢獻者頁面改版**：分開「開發者／贊助者／貢獻者／資料來源」，各自有顏色
  （開發者黃、贊助者水藍、貢獻者綠），並顯示 **Minecraft 頭像**。
  - 頭像走原版的 profile 與皮膚管線（Mojang 自己的伺服器），
    不經過第三方頭像網站。抓不到就是預設皮膚，不會變成破格。
  - 名單仍然是資料檔，加人不必改程式。

## [1.5.0] - 2026-08-19

### 修正

- **GitHub 上改完的翻譯遊戲裡不會更新。** 兩個原因：
  - 同步來源把 jsDelivr 排在第一位，而它的快取最長 12 小時，
    又從來不會「失敗」，所以永遠退不到即時的 raw 那條。現在 raw 在前、
    CDN 當備援（扛 raw 被擋或流量受限的情況）。
  - F6 的「重新載入譯文檔」只重讀本機檔案，讀到的還是同一份舊快取。
    譯文來源設在 GitHub 時，按鈕現在會變成「從 GitHub 重新抓譯文」，
    真的重新連線；抓取在背景進行，畫面不會卡住。
- **就地取代模式下欄位對不齊**（數值黏在標籤旁邊、右邊空一大塊）。
  差額被加到<b>行尾的留白邊距</b>而不是標籤與數值中間的對齊空白——
  整行寬度是對的，所以量寬度的自我檢查抓不到，只有肉眼盯著才看得出來。
  現在只挑「後面還有文字」的空白。置中的行也一併修好了。
- **F6 的說明文字掉到卡片外面。** 卡片高度漏算了最後一列的說明。

### 新增

- **四個框都可以自由擺位**：翻譯面板、NPC 對話、任務追蹤、NPC 名牌。
  「調整面板位置」改成<b>四個框同時顯示</b>，直接看得出會不會互相擋到——
  一次只調一個的話，根本不知道對話框會不會壓到任務追蹤。
- **`tools/validate.py`：譯文的機械檢查**，並接上 GitHub Actions，
  每個 PR 自動跑。擋掉佔位符數量不符、佔位符寫錯、換行數不符、
  材質包符號被直接貼進譯文、地名被翻掉——這些無論翻得好不好都一定錯。
- `CONTRIBUTING.md` 補上三道審核關卡的說明。

### 調整

- F6 的行距會依畫面高度自動調整，說明文字有呼吸空間。

## [1.4.1] - 2026-08-19

### 修正

- **對話框、名牌、任務追蹤框只有框沒有字。** 三個小框都用
  `drawString(..., 0xFFFFFF)` 傳顏色，但那個參數是 **ARGB**——`0xFFFFFF`
  的 alpha 是 `0x00`，文字照常排版卻完全透明。
  - 同一個錯誤讓 F6 的說明文字、貢獻者頁、位置調整頁的文字全部隱形。
    先前「顏色設定需要註解」的回報其實是註解一直都在，只是看不見。
  - `TooltipPanel` 沒受影響，因為它走原版 `renderTooltip`，alpha 由原版補。
  - 顏色集中到 `Colors`，之後新增繪製程式碼不會再漏掉 alpha。
- 小框會濾掉整行都是圖示的裝飾行（名牌底圖的 `banner/pill`、血條）。
  我們自己畫了背景，再疊一張原本版面的底圖只會蓋掉譯文。
  圖示<b>混在文字裡</b>的行仍然整行保留。
- **翻譯面板比原文多出 NORI／WYNNPOOL 區塊。** 這些行是 Nori、Wynnpool
  兩個第三方模組加進事件清單的，但 Wynntils 實際畫出來的 tooltip 不含它們，
  於是面板比原文多了好幾行、對不齊。現在會濾掉。
  - 清單放在 `third-party-sections.json`，把同名檔案放進
    `config/wynnchayuan/` 就能自己增修；想看這些區塊的譯文就把它清空。

### 其他

- `overlay-debug-*.txt` 診斷檔改成只在「收集未翻譯字串」開啟時才寫。

## [1.4.0] - 2026-08-19

### 新增

- **就地取代模式**：物品 tooltip 的翻譯多了一種呈現方式，直接把原文換成譯文，
  不再另開面板。F6「顯示 → 物品翻譯」可在 `另開面板 / 就地取代 / 關閉` 之間切換。
  - 兩種模式共用同一套逐片段替換，圖示、顏色與欄位對齊的保真程度完全相同，
    差別只在要不要保留英文原文。
  - `另開面板` 仍是預設值：留著原文才方便跟老玩家溝通裝備名稱。
- F6「翻譯」卡新增「對話／追蹤小框」開關，與物品翻譯拆開各自控制。
  以前兩者綁在同一個開關，想關掉小框就會連物品翻譯一起關掉。

### 相容性

- 舊設定檔的 `showPanel` 會自動轉換：`true` → `另開面板`，`false` → `關閉`，
  小框開關沿用同一個值。不需要手動重設。

## [1.3.0] - 2026-08-19

### 新增

- **介面文字收集**（F6 →「收集介面文字」，預設關）：公會選單、任務書、
  製作台這類伺服器自製 GUI 的文字不在官方資料裡，只能靠打開那些畫面收集。
  做成獨立開關是因為平常掃背包只會累積大量「CDN 已經有」的裝備文字，
  把真正缺的 GUI 字串淹掉
- `gui.json` 翻譯檔（34 條，取自公會週目標、任務書、技能樹的實際畫面）
- 停留時間改成**可直接輸入秒數**（0 = 持續顯示），不必一直點循環
- 調整面板位置時，示意框上方加了標題列；儲存後會顯示確認

### 變更

- 框線顏色的說明改成「16 進位色碼，例如 #6FA8D8」

## [1.2.1] - 2026-08-19

### 變更

- F6 標題列與卡片之間的留白加大
- 小框內容整理過程會寫進 `overlay-debug-*.txt`（前三次），
  用來查「框有出現但裡面空白」是卡在哪一步

## [1.2.0] - 2026-08-19

### 修正

- **小框有出現但裡面是空的**：對話、名牌、追蹤這三個框沿用了遊戲原本的
  排版偏移字元（`minecraft:space`），但那些偏移是為<b>原本的版面</b>算好的
  （3D 名牌置中、tooltip 欄位對齊），搬進自己畫的框只會把文字推到框外。
  這三個框現在會先把排版偏移與換行整理掉再顯示
- 多行的名牌（`{#}
Blacksmith
{#}{#}`）現在正確拆成多行顯示

### 變更

- F6 改成卡片式版面（顯示／翻譯／資料三張卡），配色沿用主題色
- 譯文檔清單改由 `translations/_index.json` 決定——
  **新增翻譯檔不必再改 Java**，而且使用者可在自己的 config 加自訂檔案
- 譯文載入順序改依清單，專用檔（npc、quest）排在通用檔之後才能勝出

### 新增

- `CONTRIBUTING.md`：翻譯參與指南，含不需要 Git 的網頁編輯流程
- `tools/progress.py`：翻譯進度與「接下來翻哪些」建議
- `tools/merge_captured.py`：把玩家收集到的字串併進工作檔
- GitHub issue 範本：提交翻譯／提供收集結果／回報問題

## [1.1.0] - 2026-08-19

### 新增

- 任務對話、任務追蹤、NPC 名牌都加上 `shown` / `noMatch` 診斷計數，
  可從 `captured.json` 的 `_meta.events` 分辨「沒讀到」與「讀到了但沒譯文」
- 新增 `quest.json` 翻譯檔（任務對話與追蹤）

### 變更

- 所有小框（對話、追蹤、名牌）改用可設定的主題色，預設 `#6FA8D8`
- F6 頁面重新排版：標題與分區標頭套用主題色，新增色碼輸入框

### 修正

- **對話框永遠不會消失**：內容設進去之後沒有任何機制清除，走開也留在畫面上。
  改為「最後一次更新後隔一段時間自動收起」，時間可設定
- NPC 名牌譯文對不上：實際名牌是多行的（`{#}
Blacksmith
{#}{#}`），
  字典裡缺 NPC 名稱。新增 `npc.json`

- 自行插入的對齊字元用錯字型，畫面上會出現方框
- 置中的行補滿差額會被推到右邊，改為只補一半
- 以符號結尾的行（`1 x Doom Stone ◆◆◆`）被誤判成「標籤 + 數值」
- 個資過濾漏掉上線／下線通知（`AnDrEw_ has just logged in!`）

## [1.0.0] - 2026-08-19

首個公開版本。

### 新增

- **物品翻譯面板**：在原本的 tooltip 旁邊另開一塊顯示譯文，原文完全不動
- **逐片段替換**：只換掉查得到的文字，符號、外框、顏色、對齊全部保留原樣
- **對齊自動修正**：譯文寬度與原文不同時，自動調整 Wynncraft 的對齊空白字元
- **NPC 名牌**：可選「注視時顯示」，保留原文以便與其他玩家溝通
- **任務對話與追蹤**：畫面下方與左側的獨立小框
- **翻譯工作檔**：內建 9,000 餘條待翻條目，首次啟動自動產生於 config 目錄
- **字串收集**：把沒翻到的句子記進 `captured.json`，並過濾玩家個資
- **設定畫面**（F6）：面板開關、位置、間距、名牌模式、重新載入譯文

### 已知限制

- 逐片段替換無法跨片段調整語序
- 極長的 tooltip 可能超出畫面，建議改用固定定位
