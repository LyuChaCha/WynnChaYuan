---
name: 提交翻譯
about: 不想用 GitHub 網頁編輯的話，把翻好的內容貼在這裡
title: '[翻譯] '
labels: translation
---

**檔案**（不確定就留空）：例如 `ui-labels.json`

**翻譯內容**（一行一條，用 `=` 分隔）：

```
Combat Level = 戰鬥等級
Health Regen = 生命回復
```

> 佔位符 `{#}` `{~}` `{p}` 要原樣保留，數量必須跟原文一樣。
> `{p}` 是地名，永遠不翻。
