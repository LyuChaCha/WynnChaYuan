# 參與翻譯

English: **[CONTRIBUTING.en.md](CONTRIBUTING.en.md)**

> **加入團隊前請先連絡 LyuChaCha**，避免各翻各的：[給翻譯團隊](docs/for-translators.md)
> —— 技能名稱只翻一次、`{~1}` 指名數值、詞條的三種寫法。

翻譯檔就是純 JSON，只要把 `dst` 填上中文。**不需要會寫程式。**

目前進度：**135 / 9,132**（1.5%）。

---

## 最快的方式：在 GitHub 網頁上改

不用安裝任何東西，不用會 Git，**也不用先 fork**——按下鉛筆時 GitHub 會自動處理。

1. 到 [translations 資料夾](src/main/resources/assets/wynnchayuan/translations)
2. 點你想翻的檔案（**建議從 `ui-labels.json` 或 `npc.json` 開始**，那些每天都看得到）
3. 按右上角的 **鉛筆圖示**（Edit this file）
4. 找到 `"dst": ""`，把中文填進去
5. 拉到最下面，按 **Commit changes**
6. 跳出來的視窗選 **Create a new branch and start a pull request**，再按 **Propose changes**
7. 下一頁按綠色的 **Create pull request** ← **這一步最容易漏掉**

> ### 已經 fork 了但不知道下一步？
>
> **fork 只是把檔案複製到你自己的帳號底下，改了不會回到這裡。** 要讓翻譯生效，
> 得再開一個 Pull Request：
>
> 1. 到<b>你自己那份</b> repo（網址是 `github.com/你的帳號/WynnChaYuan`）
> 2. 上方會有一條 **This branch is N commits ahead** 的提示，點旁邊的
>    **Contribute** → **Open pull request**
> 3. 確認左邊是 `LyuChaCha/WynnChaYuan : main`、右邊是你的分支
> 4. 按 **Create pull request**
>
> 沒有那條提示的話，代表你 fork 之後還沒有做任何修改。
>
> 送出之後會自動跑格式檢查，綠燈就等著被合併，紅燈的話點進去看它說哪裡錯。

合併之後，**所有裝這個模組的人下次進遊戲就會拿到你的翻譯**——不需要重新下載模組。

> 大檔（`gear-weapon.json` 有 550KB）在網頁編輯器上會有點慢，
> 用瀏覽器的搜尋功能（Ctrl+F）找關鍵字比較快。

---

## 從哪裡開始翻

檔案依「玩家看到的頻率」排序，**由上往下翻效益最高**：

<!-- 進度:開始 -->
更新於 2026-08-27。

| 語言 | 進度 | 已翻 / 總數 |
|---|---|---:|
| `zh_tw` 繁體中文 | ███░░░░░░░ 33.0% | 8,885 / 26,954 |
| `de_de` Deutsch | ░░░░░░░░░░ 0.0% | 0 / 26,765 |
| `es_es` Español | ░░░░░░░░░░ 0.0% | 0 / 26,765 |
| `fr_fr` Français | ░░░░░░░░░░ 0.0% | 0 / 26,765 |
| `ja_jp` 日本語 | ░░░░░░░░░░ 0.0% | 0 / 26,765 |
| `ko_kr` 한국어 | ░░░░░░░░░░ 0.0% | 0 / 26,765 |
| `ru_ru` Русский | ░░░░░░░░░░ 0.0% | 0 / 26,765 |
| `zh_cn` 简体中文 | ░░░░░░░░░░ 0.0% | 0 / 26,765 |

每一種語言**還缺哪些檔案**見 [docs/PROGRESS.md](docs/PROGRESS.md)。<br>Per-language breakdown: [docs/PROGRESS.md](docs/PROGRESS.md).
<!-- 進度:結束 -->

不確定翻什麼就跑這個：

```bash
python tools/progress.py --next 30
```

---

## 三種佔位符：原樣保留，位置可調

| 佔位符 | 代表 | 例子 |
|---|---|---|
| `{#}` | 材質包符號 | `{#} Mana Cost: {~}` → `{#} 魔力消耗: {~}` |
| `{~}` | 數值 | `- +{~} Emeralds` → `- +{~} 綠寶石` |
| `{p}` | 地名（**永遠不翻**） | `{p} Citizen` → `{p} 市民` |
| `{u}` | 玩家自己的名字 | `Great job, {u}!` → `做得好，{u}！` |

### 同一個標籤有三種寫法

遊戲對同一個詞條會用三種英文標籤，各自對應一種中文。**照這張表填，不要自己改規則**——
這裡已經來回改過三次，每改一次遊戲裡就錯一輪。

| 英文標籤 | 什麼時候出現 | 中文 | 例 |
|---|---|---|---|
| `X` | 實數與百分比**都會用到**這個標籤 | 名稱 | `火屬性傷害` |
| `X%` | 我們自己的機制：數值帶 `%` 時優先查這個 | 名稱 + `百分比` | `火屬性傷害百分比` |
| `X Raw` | 英文自己就寫了 Raw | 名稱 + `值` | `火屬性傷害值` |

```json
"Fire Damage":     "火屬性傷害",
"Fire Damage%":    "火屬性傷害百分比",
"Fire Damage Raw": "火屬性傷害值"
```

**`X` 千萬不要寫成「…百分比」。** 遊戲裡 `Thunder Damage: +1-8` 這種實數走的
也是 `X`，寫成百分比就會看到「雷屬性傷害百分比 +1-8」。判斷看的是**數值本身**
而不是整行有沒有 `%`——`+282 [53.8%]` 後面那個是詞條品質，不是數值。

`X%` 中間**不能有空格**：機制是直接在標籤後面接 `%`，寫成 `"Fire Damage %"`
永遠比對不到。

只在**真的兩種都有**的時候加 `X%`。近戰反傷、遠程反傷本來就只有百分比，
魔力回復、生命竊取、中毒、擊退、寶物加成本來就只有一種，替它們多開一個
只會讓後面的人以為遊戲裡真的有兩種。

目前確認兩種都有的，只有這些：

- `Health Regen`
- 五系的 `X Damage`、`X Main Attack Damage`、`X Spell Damage`
- `Elemental Damage`、`Elemental Spell Damage`、`Neutral Damage`

**要加新的，先在遊戲裡看到同一個標籤同時出現過實數與百分比再加。**
`ui-labels.json` 裡已經照這個規則整理好，也用底線開頭的分段標題分好類了
（那些鍵程式會跳過，只是給人看的）。

### 佔位符

### 一句裡有兩個以上的 `{~}` 怎麼分

**照順序**：譯文裡第一個 `{~}` 拿原文的第一個數值，第二個拿第二個。
每一條都有 `_raw` 欄位，裡面是<b>還沒抽掉數字</b>的原文，對照著看就知道誰是誰：

```json
"src":  "Allies within {~} blocks gain {~} of the health you gain...",
"_raw": "Allies within 16 blocks gain 100% of the health you gain..."
```

中文語序跟英文相反、需要調換時，用 **`{~1}` `{~2}`** 指名要第幾個：

```json
"src": "+{~} to {~}",
"dst": "最多 {~2}，最少 {~1}"
```

沒有編號的維持照順序，兩種可以混用。

### 顏色不對的時候：`{c1}` `{c2}` `{/}`

譯文的顏色平常是**猜**出來的——拿原文有顏色的那一段，到譯文裡找一樣的字面。
技能名、地名這種保留英文的詞找得到，翻成中文的就找不到，那一段只好掉回底色。
整行只有一個顏色的還救得回來（照位置對），**一行裡混了兩三種顏色的就沒辦法**。

猜不到的時候，唯一知道答案的是你。所以可以直接寫出來：

| 寫法 | 意思 |
|---|---|
| `{c1}`–`{c9}` | 用原文的第 N 個顏色 |
| `{c:#FF55FF}` | 自己指定色碼 |
| `{c:gold}` | 自己指定原版顏色名稱（只認 Minecraft 那十六個） |
| `{/}` | 到此為止，回到這一段原本的顏色 |

```json
"src": "[Cave Completed]\nGrook's Nest\n- Rewards:",
"dst": "{c1}[洞穴完成]{/}\n{c2}Grook 巢穴{/}\n{c3}- {c4}獎勵：{/}"
```

**編號是怎麼算的**：`{cN}` 指的是**這一條原文**用到的第 N 個顏色，
依第一次出現的順序編號。搬的是整個樣式，所以粗體、底線、Wynncraft 那些
非原版的色碼都會一起帶過去；遊戲哪天改了調色盤，譯文自動跟著改。

**哪一個編號是哪一個顏色**，看診斷檔 `config/wynnchayuan/majorid-debug.txt`
裡的「可用的顏色」那一段（F6 → 診斷開著才會寫）：

```
=== 可用的顏色 1 ===
  譯文：[洞穴完成]
    {c1}  #55FF55
    {c2}  #FFFFFF 粗體
    {c3}  #FF55FF
```

**幾件要注意的事**

- 佔位符**只寫在譯文**，`src` 那半是遊戲送來的字，寫進去會永遠查不到。
- 一個 `{cN}` 管到 `{/}`、下一個 `{cN}`、或**這一行結束**為止。
  忘了寫 `{/}` 只會影響那一行，不會整塊被染色。
- 編號寫超出範圍就**當作沒寫**，那一段回去走猜的那條路——顏色不對可以忍，
  整句變回英文不行。
- Wynncraft 大部分的顏色不在原版那十六個裡面。**不要自己挑一個相近的**，
  用 `{cN}` 直接搬原文的。
- 只在顏色真的錯掉時才寫。猜得對的地方不必動，多寫反而讓譯文檔難讀。

如果你只是要讓中文自己強調一下（原文根本沒有對應的色段），
用 Minecraft 原本的 `§` 格式碼也可以：`"§c警告§r：這會消耗你的魂"`。

### 技能名稱只要翻一次

技能說明裡到處都在提別的技能（`Bash will hit a second time...`）。
**不用逐條翻**——把 `ability.json` 裡 `Bash` 那一條的名稱翻好，
所有提到它的敘述會自動換成譯名。

所以在敘述裡看到技能名稱時，**原樣留著英文就好**。

**數量必須一樣**，位置可以依中文語序調整。少一個或多一個，那一行就會整行放棄翻譯，
顯示原文——寧可不翻，也不要畫出錯位的東西。

`{p}` 特別值得注意：`{p} Citizen` 翻一次，**137 個地名全部適用**，
而且地名本身永遠保持英文（跟其他玩家溝通時對得上）。

## 怎麼確認自己翻得對，而不是硬填

送出前有三道關卡，前兩道你自己就能做完。

### 第一道：機器檢查（自動）

送出 PR 之後 GitHub 會自動跑 `tools/validate.py`，擋掉<b>無論翻得好不好都一定錯</b>的東西：

| 檢查 | 為什麼要擋 |
|---|---|
| 佔位符數量不符 | 少一個，那個數字或符號在遊戲裡會憑空消失 |
| 佔位符寫錯（`{ ~ }`、全形括號） | 會原樣顯示成文字 |
| 顏色佔位符寫錯（`{c0}`、`{c:purple}`） | 會原樣印出 `{c0}`，看起來像亂碼 |
| 換行數不符 | 技能說明是固定幾行的排版，行數變了整塊會擠掉 |
| 譯文含材質包符號 | 符號由程式填回原位，手寫的碼位一定是錯的 |
| 地名被翻掉 | 翻了就跟其他玩家對不上話 |

自己先跑一次比較快：

```bash
python tools/validate.py ui-labels.json
```

它也會給警告（譯文和原文相同、比原文長很多、首尾空白不一致）。
警告不會擋合併，但請自己確認是不是刻意的。

### 第二道：進遊戲看實際效果（你自己）

**這一步不能跳。** 機器只看得出格式，看不出「翻出來會不會超出框」、
「語氣對不對」、「這個詞在遊戲脈絡下是不是這個意思」。

1. 遊戲裡按 **F6** → 把「譯文來源」切成 **本機**
2. 改 `config/wynnchayuan/translations/` 底下的檔案
3. **F6 → 重新載入本機譯文檔**
4. 去看那個物品／NPC／介面，確認畫面上長得對

用「另開面板」模式看最清楚——原文和譯文並排，一眼就知道有沒有翻錯行。

> 譯文來源設在 **GitHub** 時，那顆按鈕會變成「從 GitHub 重新抓譯文」，
> 用來確認別人剛合併的翻譯。

### 第三道：人工審核（維護者）

<details>
<summary>維護者：怎麼合併 PR</summary>

**只用綠色的 Merge pull request，不要用「bypass rules」那顆。**

紅色／bypass 那顆的意思是「我知道檢查沒過，但我硬要合併」。已經因此混進
兩批壞掉的譯文——其中一筆把「吸取 {~} 點魔力，每 {~} 秒」寫成
「每 {~} 秒吸取 {~} 點」，佔位符是<b>依序</b>填回的，所以兩個數字會對調，
玩家看到的數值是錯的，而且畫面上完全看不出來。那種錯只有機器擋得住。

正常流程：

1. PR 送出後自動跑檢查（譯文的 PR 不需要核准就會跑）
2. 綠燈 → **Merge pull request** 直接可以按
3. 紅燈 → 點 **Details** 看它說哪一條錯，請作者修，或自己 push 修正到那個分支

檢查沒跑的話，多半是動到了 `tools/` 的 PR——那是程式碼，需要維護者按
**Approve and run** 才會跑，那是刻意的。

</details>

所有 PR 都要有人看過才會合併。審的是機器看不出來的部分：

- 這個詞在 Wynncraft 的脈絡下是不是這個意思（不是字典上的意思）
- 有沒有跟既有譯文不一致（同一個詞在不同檔案翻成兩種說法）
- 是不是整批機翻沒有校對過

**明顯是機器直翻、沒有進遊戲對照過的 PR 會被退回。** 這不是刁難：
錯的譯文比沒有譯文糟，因為玩家會照著它做出錯誤判斷，而且沒人知道要去改。

拿不準的時候，**保留原文比猜一個好**。`dst` 空著代表「還沒翻」，
模組會顯示原文，完全沒問題。

---

## 專有名詞先查對照表

碰到不確定的詞，**先看 [GLOSSARY.md](GLOSSARY.md)，不要自己另外想一個**。
同一個詞在不同檔案翻成兩種說法，玩家會以為是兩個不同的東西。

有些詞照字面翻是錯的：

| 原文 | 正確 | 常見的翻錯 |
|---|---|---|
| Reflection | 遠程反傷 | ~~反射~~ |
| Thorns | 近戰反傷 | ~~荊棘~~ |
| Scale | 詞綴 | ~~規模／比例~~ |

`tools/validate.py` 會檢查譯文與對照表是否一致，不一致就在 PR 上提出警告。
要改某個詞的譯法，**先改 GLOSSARY.md**，讓所有人一起跟著改。

---

## 翻譯慣例

完整規則見 [GLOSSARY.md](GLOSSARY.md)，這裡只列最常碰到的：

- **裝備與物品名稱保留英文**。那是專有名詞，翻了反而對不上社群討論與 wiki
- **地名保留英文**（用 `{p}` 自動處理）
- **技能名稱**建議保留英文或附註，例如 `Meteor` → `Meteor（隕石）`
- 語氣照原文。Wynncraft 的 NPC 講話很口語，不用翻得太正式

---

## 不想碰 GitHub？

開一個 [Issue](https://github.com/LyuChaCha/WynnChaYuan/issues/new)，把你翻好的內容貼上來就好，格式像這樣：

```
Combat Level = 戰鬥等級
Health Regen = 生命回復
```

有人會幫你併進去。

---

## 幫忙收集沒被翻譯的句子

任務對話與 NPC 名稱**沒有官方資料可抓**，只能靠玩家在遊戲裡實際遇到。

模組會自動把沒翻到的句子記進
`config/wynnchayuan/captured.json`。玩一段時間後把這個檔開 Issue 附上來，
就能變成大家共用的待翻條目。

> **附上前請先看一眼。** 模組會過濾玩家名稱、好友名單、座標，
> 但 Wynncraft 的通知格式很多，可能有漏網。看到別人的名字就刪掉那一條，
> 順便回報一下，我們補過濾規則。

維護者這樣併：

```bash
python tools/merge_captured.py captured.json --dry-run   # 先看會加什麼
python tools/merge_captured.py captured.json
```

---

## 給開發者

```bash
gradle build              # 會自動跑五組驗證
python tools/validate.py  # 譯文檔的機械檢查
```

Wynntils 沒有 Maven 座標，要自行從
[Modrinth](https://modrinth.com/mod/wynntils/versions) 下載 **fabric 版**放進 `libs/`。

改動 `tools/build.py` 的參數化規則時特別注意：**mod 端有一份對應的實作**
（`LineParts` / `GlyphSplitter`），兩邊算出的模板必須完全一致，
否則遊戲裡會靜默查不到譯文。`TemplateParityTest` 就是在擋這件事。
